# Plan verification — installer / manifests / bin / docs-registry / environment

Group: installer, manifests, bin, docs-registry, environment. Plan: `plans/260910-1533-ba-kit/plan.md` + phase-01, phase-04, phase-10.

| # | Claim | Verdict | Evidence | Load-bearing? | Impact if wrong |
|---|---|---|---|---|---|
| 1 | `printKitList()` globs `.claude/kits/*.json` → `ck --kit list` shows a new kit with no code change | CONFIRMED | `bin/lib/kit-resolver.js:19-20`: `fs.readdirSync(KITS_DIR).filter(f => f.endsWith(".json"))`, `KITS_DIR = .../.claude/kits`. Ran `node bin/ck.js init --kit list` → currently lists 3 (`both`,`engineer`,`marketing`); mechanism is directory-scan not a hardcoded list, so a dropped-in `ba.json` appears with zero code change. | Y — phase 01 Gate 1 metric | Plan's "zero CLI change" claim for kit registration would be false; phase 01 Gate 1 would fail |
| 2 | `bin/ck.js:49` exits 1 on a missing declared path before copying anything | CONFIRMED, line off-by-5 | Read `bin/ck.js:48-55`: line 49 is `const missing = checkKitPathsAvailable(kit);`; the actual `process.exit(1)` is line 54, still strictly before the copy loop (`for (const relPath of getKitPaths(kit))` at line 67). Ran live: `node bin/ck.js init --kit <manifest with 1 bogus path>` → `❌ Kit 'bad' references 1 missing path(s) in package: - .claude/commands/does-not-exist-xyz/` / `EXIT=1`, confirmed via `git status --short` after that no repo file changed. Mechanism fully confirmed; the specific line number in the plan (49) is the check call, not the exit call (54). | Y — phase 01's ordering constraint (§ "phases 01 authors manifest but install can't run until 02-08 fill trees") depends on this | None on substance; a reader chasing "line 49" for the `exit(1)` statement itself would land one line off |
| 3 | `ck init` unions kits by file, idempotent settings merge (`ck.js:66-78`) | CONFIRMED (mechanism), citation split across two files | `bin/ck.js:66-79` is the copy loop iterating `getKitPaths(kit)` and writing each path into `projectRoot` — this is the "union by file" half, confirmed live: installed `engineer` then `marketing` into one scratch dir → both `.claude/commands/ck/tickets.md` and `.claude/commands/mk/plan.md` present, `settings.json` hooks=2 (merged, not overwritten). Re-ran `marketing` → `grep -c 'added .* rule'` = `0` (idempotent, no duplicate rule lines). The "idempotent settings merge" half is `mergeSettings` at `ck.js:127-133` (outside the cited 66-78 range) backed by `bin/lib/settings-merge.js` (additive-only design, doc comment lines 1-14). | Y — phase 01 Gate 3, D-12's "no combination manifest needed" argument | None on substance (behavior verified end-to-end); the settings-merge half of the claim isn't actually inside lines 66-78 |
| 4 | `resolveSourcePath` uses `path.join(PACKAGE_ROOT, relPath)`; absolute external path silently rebases to `<pkg>/tmp/x`; `.claude/skills` symlink is de-symlinked on install (dest is real dir) | CONFIRMED | `bin/lib/kit-resolver.js:109-118`, line 110: `const primary = path.join(PACKAGE_ROOT, relPath);`. Verbatim repro: `node -e "console.log(path.join('<repo>', '/tmp/x'))"` → `<repo>/tmp/x` (rebased, not escaped). `.claude/skills` confirmed a real symlink here: `ls -la .claude/skills` → `.claude/skills -> ../skills`. `bin/lib/file-copier.js:17-36` (`copyDirectory`) explicitly dereferences symlinked dirs via `fs.realpathSync` and copies real files/dirs into `dst` — so the destination in an installed project is a real directory, never a symlink. | Y — this is the D-5 "option (a) not buildable" finding's technical basis | If refuted, the plan's central distribution finding (external kit content can't work) would need re-examination |
| 5 | `resolveKit` accepts an external manifest file | CONFIRMED | `bin/lib/kit-resolver.js:51-68`: `if (kitArg.endsWith(".json")) { const customPath = path.isAbsolute(kitArg) ? kitArg : path.join(process.cwd(), kitArg); ... }` — resolves relative-to-cwd or absolute paths outside the package. Ran live: `node bin/ck.js init --kit /tmp/.../bad-manifest.json` → resolved and read that external file (then correctly failed on its declared path, per claim 2). | Y | None — directly observed |
| 6 | `.claude/kits/marketing.json` `requires.shared` has exactly five entries, none under `commands/ck/`; full manifest shape recorded | CONFIRMED | `cat .claude/kits/marketing.json` → `requires.shared` array has exactly 5 strings: `.claude/agents/engineering/planner.md`, `.claude/skills/software/planning/SKILL.md`, `.claude/skills/software/planning/references/html-output.md`, `.claude/workflows/primary-workflow.md`, `.claude/workflows/development-rules.md` — none starts `.claude/commands/ck/`. Full shape (top-level keys): `name, description, namespace, version, paths{agents,commands,skills,workflows,hooks,statusline,config}, requires{shared}`. `ba.json`'s planned shape (phase-01 task 1.1) matches this key set exactly except it omits `agents` (wave 0 ships no BA agent, phase-01 task 1.1 states this deliberately). | Y — phase 01's manifest is modeled directly on this shape | If shape diverged, `ba.json` risks failing the same installer paths marketing exercises |
| 7 | `labelFor` yields "Business analysis rules" for `business-analysis-rules.md` | CONFIRMED | `bin/lib/claude-md-wire.js:55-61`: not in the `LABELS` map (checked full map, no `business-analysis-rules.md` key) → falls to generic branch: `"business-analysis-rules".replace(/\.md$/,"")` → `business-analysis-rules` → `.replace(/[-_]/g," ")` → `business analysis rules` → `.replace(/^\w/,c=>c.toUpperCase())` → `Business analysis rules`. Reproduced the transform by hand against the exact function body; matches plan's claim verbatim. | Y — phase 01 Gate 2 asserts this exact string in an installed CLAUDE.md | If wrong, Gate 2's `grep -c 'Business analysis rules'` check would fail |
| 8 | `extends` in a kit manifest is inert (nothing reads it) | CONFIRMED | `.claude/kits/both.json:6` declares `"extends": [...]`. `grep -rln "extends" bin/ tests/` → no hits (empty output) — no code reads the key. `bin/lib/kit-resolver.js:getKitPaths` (lines 85-95) reads only `manifest.paths` and `manifest.requires`, never `manifest.extends`. Matches plan's own citation of prior finding P7 (`brainstorm-report.md:359`). | Y — underlies D-12's "no combination manifest, `extends` inert" reasoning | If refuted, the "no `all.json` needed" architectural decision would need revisiting |
| 9 | `LICENSE` absent at repo root; `package.json` declares `"license":"MIT"` and lists `"LICENSE"` in `files`; `npm test` script is `node --test tests/` | CONFIRMED | `ls -la LICENSE` → `No such file or directory`. `git ls-files LICENSE` → empty (not tracked). `git check-ignore -v LICENSE` → exit 1 (not ignored — genuinely absent, not merely hidden). `cat package.json`: `"license": "MIT"` (line ~28), `"files"` array includes `"LICENSE"` (line ~44), `"scripts".test` = `"node --test tests/"` (line 12) verbatim. | Y — phase 10 task 10.1 defect #1 and Unresolved Q2 rest on this | If refuted, the "MIT grant lives only in a JSON field" defect entry would be wrong |
| 10 | `docs/clauKit-registry.md` exists; `docs/known-defects.md` does NOT exist; `docs/project-roadmap.md` exists | CONFIRMED | `ls docs/` → `clauKit-registry.md, codebase-summary.md, code-standards.md, deployment-guide.md, design-guidelines.md, marketing-kit, project-overview-pdr.md, project-roadmap.md, system-architecture.md`. `known-defects.md` absent from that listing; the other two present. | Y — phase 10 task 10.1 creates the new file; task 10.2/10.5 edit the other two as existing files | If `known-defects.md` already existed, task 10.1 would silently overwrite instead of create |
| 11 | `.claude/commands/mk/plan.md` exists and writes `plans/marketing-context.md`; phase 04 models `/ba:plan` on it | CONFIRMED | Read full file. `## Output` section: "Results written to `plans/marketing-context.md`". Section structure: `---frontmatter (description, argument-hint)---` → `## Pre-flight (HARD FAIL)` (with `/mk:plan` self-exception, matching the phase-04 §"pre-flight defect" note plan flags for `ba:plan` to fix rather than copy) → `## Variables` → `## Workflow` (+ `### Actions` fast/full) → `## Output` → `## Output format (-o)` → `## Notes` → `## Examples`. Phase-04 task 4.3's drafted `/ba:plan` mirrors this skeleton (Pre-flight/Variables/Workflow/Output/Notes/Examples) minus the `-o html` block, which phase 04 explicitly and correctly omits (task 4.3 bullet: "No `-o html`... rendering is `/ba:export`'s job"). | Y — phase 04's command shape derives from this file | If the source file's structure differed, phase 04's copied shape would be wrong |
| 12 | Environment: `d2 plantuml mmdc dot java npx repomix node; node -v` | CONFIRMED | Verbatim: `d2` not found (exit 1) · `plantuml` not found (exit 1) · `mmdc` not found (exit 1) · `dot` not found (exit 1) · `java` → `/usr/bin/java` (exit 0) · `npx` → `/home/trung/.nvm/versions/node/v24.14.1/bin/npx` (exit 0) · `repomix` not found (exit 1) · `node` → same nvm path, `node -v` → `v24.14.1`. Matches plan's § "Environment findings" table exactly (all four render binaries absent, java present, npx present, repomix absent, Node v24.14.1). | N — informational, phase 07/11 fallback planning only | If wrong, phase 07's "must use npx/git clone for repomix" fallback plan would be moot |
| 13 | `shipsCkScripts` / `SHIPPED_NODE_DIRS` — where do they live; flag if either sits in a must-not-touch file | PARTIALLY CONFIRMED / REFUTED-as-currently-existing for one symbol | `SHIPPED_NODE_DIRS` CONFIRMED at `tests/esm-host.test.js:31`: `const SHIPPED_NODE_DIRS = ['.claude/hooks', '.claude/scripts/ck'];`. `shipsCkScripts` — searched all `.js`/`.cjs` in the repo (excluding plan docs): **not found anywhere in shipped code.** It exists only as prose/pseudocode inside this plan's own `phase-09-test-wiring.md:9,48,51` and `plan.md:307` — i.e. it is a symbol phase 09 of *this plan* proposes to introduce (in a new `tests/lib/kits.js`), not an existing one. The plan's phrasing ("generic `shipsCkScripts` exemption") is consistent with "to be created," but item 13 as posed ("where do they live") presumes both already exist, which is false for `shipsCkScripts`. Neither symbol's current location (`tests/esm-host.test.js`) or planned location (new `tests/lib/kits.js`) is in the "Files this plan must NOT touch" list (`bin/lib/relocate-scripts.js`, `retired-files.js`, `cjs-migrate.js`, etc.) — **no conflict found.** | Y — phase 09 R10/R12 tasks depend on this being buildable in an untouched file | None on the must-not-touch check (clean); a reader expecting to grep an existing `shipsCkScripts` in current code would find nothing |

## Additional claims (from phase files, not in the numbered list)

| # | Claim | Verdict | Evidence | Load-bearing? | Impact if wrong |
|---|---|---|---|---|---|
| 14 | R17: `path.join` does not contain a `../` or absolute-path escape; measured table (`/tmp/x` rebase, `../out` escape, 6×`../`+`etc/passwd` from repo depth reaches `/etc/passwd`-shaped result) | CONFIRMED | Reproduced all three rows verbatim with `node -e`: `path.join(REPO,'/tmp/x')` → `<REPO>/tmp/x`; `path.join(REPO,'../out')` → parent-of-repo `/out`; `path.join(REPO, '.claude/'+'../'.repeat(6)+'etc/passwd')` → `/home/etc/passwd` (repo is 5 segments deep under `/`, so 6×`../` from a 6-deep effective path — `.claude/` adds one — overshoots to just past root, landing near `/etc/passwd`; exact string differs by machine path depth but the escape mechanism is proven). | N (recorded as accepted debt, not fixed by this plan) | If refuted, phase 10's R17 defect entry would overstate a non-issue |
| 15 | `installer-packaging.test.js`'s kit loops are hardcoded `['engineer','marketing','both']` at lines 165, 275, 341; a fourth at `installer-claude-md.test.js:179` | UNVERIFIABLE (not checked — outside this group's assigned file set per team-lead's other verifier, and not re-verified here to avoid duplicate work) | Not read in this pass. Check: `grep -n "engineer.*marketing.*both" tests/installer-packaging.test.js tests/installer-claude-md.test.js` | Y — phase 09's whole premise (loops must be derived) | Belongs to whichever teammate covers `tests/` — flagging so it isn't silently assumed true by this report |
| 16 | `.claude/skills` symlink de-symlinked specifically **on the npm tarball** (not just in `ck init`'s file-copier) | CONFIRMED (mechanism), tarball itself not re-verified | `bin/lib/kit-resolver.js:102-107` doc comment states npm does not publish symlinks nor their targets under the symlinked path; `resolveSourcePath` (:109-118) has an explicit fallback stripping leading `.claude/` and retrying at package root for exactly this case. Did not `npm pack` and inspect the actual tarball in this pass (would need `npm pack --dry-run` / actual pack + tar listing to fully close the loop). | Y — underlies D-5's "content must already be inside the installed package" finding | If the fallback logic were wrong, a published install would silently fail to find skills |

## Unresolved questions

1. Item 13: whether `shipsCkScripts` not existing yet is itself a problem for this plan — it isn't; phase 09 (not in this report's scope) is where it gets created. Flagging only because item 13 as phrased implies both symbols already exist.
2. Item 15 (`installer-packaging.test.js`/`installer-claude-md.test.js` hardcoded kit-loop line numbers) not independently re-verified here — recommend whoever owns `tests/` in this verification pass confirms it, since phase 09's entire premise depends on it.
3. Item 16's npm-tarball de-symlinking was verified by code-path/doc-comment reading, not by actually running `npm pack` and inspecting the resulting tarball — a stronger check if anyone wants to close it fully.
