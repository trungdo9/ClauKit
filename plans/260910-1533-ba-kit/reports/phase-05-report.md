STATUS: DONE

# Phase 05 — `/ba:prd` — report

## Files created

- `skills/ba/prd/SKILL.md` (68 lines) — new, untracked
- `skills/ba/prd/references/prd-structure.md` (58 lines) — new, untracked
- `.claude/commands/ba/prd.md` (56 lines) — new, untracked
- `plans/ba-context.md` — fixture, **git-ignored** (`.gitignore:59`)
- `plans/ba/demo/entities/PRD-001.md` — fixture, **git-ignored** (`.gitignore:59`)
- `plans/ba/demo/entities/EPIC-001.md` — fixture, **git-ignored** (`.gitignore:59`)
- `plans/ba/demo/entities/EPIC-002.md` — fixture, **git-ignored** (`.gitignore:59`)
- `plans/ba/demo/entities/EPIC-003.md` — fixture, **git-ignored** (`.gitignore:59`)
- `plans/ba/demo/traceability.derived.json` — generated by `index`, **git-ignored** (`.gitignore:69`)

No staging, no commit — `git status --porcelain -- skills/ba .claude/commands/ba plans/` shows only
`?? .claude/commands/ba/prd.md` and `?? skills/ba/prd/` (fixture files ignored, not listed).

## Gate 1 — entities valid and complete

```
$ node .claude/scripts/ba/traceability.cjs validate plans/ba/demo; echo "validate=$?"
✓ validate clean
validate=0

$ node .claude/scripts/ba/traceability.cjs index plans/ba/demo --json | node -e '...'
PRD=1 EPIC=3 orphans=0 unsourced=0 epics-missing-scope=0
```
Matches gate exactly: `validate=0` · `PRD=1` · `EPIC>=3` (3) · `orphans=0` · `unsourced=0` ·
`epics-missing-scope=0`.

## Gate 2 — `roadmap` orders, invents nothing

Hand-authored § 7 into `plans/ba/demo/entities/PRD-001.md` per
`skills/ba/prd/references/prd-structure.md` § "The `roadmap` output shape" (Now/Next/Later table
over the 3 existing EPIC ids, no new entity).

```
before=4
(after adding § 7)
before=4 after=4
1
```
`before` equals `after` (roadmap created zero entities); `grep -c '^## 7' plans/ba/demo/entities/PRD-001.md` = 1.

## Gate 3 — command hygiene

```
$ awk '/^description:/{print length($0)-13}' .claude/commands/ba/prd.md
82
$ grep -c '❌ BA context not found at plans/ba-context.md' .claude/commands/ba/prd.md
1
$ grep -rn 'Activate the `' .claude/commands/ba/prd.md .claude/skills/ba/prd/; echo "activate=$?"
activate=1
```
description 82 ≤ 110; hard-fail string exactly once; `activate=1` (grep found nothing, exit 1 —
no hits). Byte-identical check: `grep -F '❌ BA context not found at plans/ba-context.md' .claude/workflows/business-analysis-rules.md` — matched, confirms same string.

## Exit gate

```
$ node .claude/scripts/ba/traceability.cjs validate plans/ba/demo && node .claude/scripts/ba/traceability.cjs index plans/ba/demo
✓ validate clean
✓ indexed 4 node(s), 3 edge(s), 0 orphan(s) → traceability.derived.json
```
`exit=0`, 1 PRD + 3 EPIC (≥3).

## Suite

```
$ node --test "tests/*.test.js" 2>&1 | grep -E '^ℹ (tests|pass|fail|skipped)'
ℹ tests 358
ℹ pass 356
ℹ fail 1
ℹ skipped 1
```
Unchanged (358/356/1/1). The 1 fail confirmed still `tests/protected-branch-guard.test.js:196`
(re-grepped the failure line in the run output) — untouched, pre-existing.

## Extra diligence (not a required gate, precedent from phase 4's STATE.md note)

Scratch-installed `ba` into a fresh git repo (`(cd $TMPD && node bin/ck.js init --kit ba)`, exit 0,
15 paths copied). Replayed both `installer-packaging.test.js` regexes (`LINK`, `TICKED`) over the
installed `.claude/` tree:
- Markdown-link check (`](target.md)`): **0 broken**, including the new `prd.md` → `SKILL.md` links and `SKILL.md` → `traceability`/`ba-context`/workflow links.
- Backticked `.claude/…` path check: 11 pre-existing misses, all in `hooks/README.md`,
  `hooks/telegram-hook-setup.md`, `workflows/development-rules.md`, `workflows/primary-workflow.md`
  — none in `prd.md` or `skills/ba/prd/**`. Note: this repo's `installer-packaging.test.js` loop
  (`tests/installer-packaging.test.js:275,332`) currently only iterates `['engineer','marketing','both']`
  — `ba` is not in that loop yet, so this check is not currently gated by the suite; ran it manually
  as a precedent from phase 4.

## Concerns

- `skills/ba/capability-map.md:9` names row 9 ("Soạn PRD") as `/ba:prd draft`, wave `W0`, and row 14
  ("Lộ trình sản phẩm") as `/ba:prd roadmap`, wave `W4`. Implemented action names are `prd`
  (default) and `roadmap`, both delivered in this wave-0 phase, per brief Task 5.3's verbatim
  `argument-hint: [prd|roadmap]`. capability-map.md is outside this phase's touch list (not
  edited); the action-name (`draft` vs `prd`) and wave (`roadmap` at W4 vs delivered at W0)
  mismatches are pre-existing text in that file, not introduced here — flagging for whoever next
  reconciles the capability map against the actual phase plan.
- `installer-packaging.test.js`'s two link/backtick checks do not include kit `ba` in their loop —
  so no automated gate currently catches a broken `.claude/skills/ba/...` link on install; the
  manual replay above found none for this phase's files.

## Incident (self-caught, reverted)

Extra-diligence step misfired: `node bin/ck.js init --kit ba --dir "$TMPD"` (unsupported `--dir`
flag) and a follow-up `--help` probe both ran with cwd = this repo instead of the scratch dir,
since `ck.js` has no `--dir`/`--help` flags and falls back to cwd. This rewrote
`.claude/metadata.json` (kit/version/date fields) and one line of
`.claude/workflows/development-rules.md`. Caught via `git status` before finishing; reverted with
`git checkout -- .claude/metadata.json .claude/workflows/development-rules.md`
(`git diff` on both now empty). Suite re-run after revert: 358/356/1/1, unchanged. The later,
correct scratch-install (`git init` a tmp dir, `cd` into it, then `node bin/ck.js init --kit ba`)
never touched this repo.

## Unresolved questions

None.
