# Verify-plan report — INSTALLER / bin / KIT MANIFESTS

Plan: `plans/260910-1533-ba-kit/plan.md`. Read-only, `mktemp -d` scratch dirs used for live installs, no repo edits.

| # | Claim | Verdict | Evidence | Load-bearing? (Y/N) — impact if wrong |
|---|---|---|---|---|
| B1 | `checkKitPathsAvailable` exits 1 on missing manifest path, before copying, ~bin/ck.js:49 | CONFIRMED | `bin/ck.js:48-55`: `const missing = checkKitPathsAvailable(kit); if (missing.length > 0) { ...; process.exit(1); }` — runs before the copy loop (`:66-79`). Fn itself at `bin/lib/kit-resolver.js:123-125`. | Y — phase-01's "manifest can't ship before phases 02-08 fill the trees" claim depends on this; wrong → ba.json could ship broken and no early guard would catch it. |
| B2 | Installing a 2nd kit unions files onto one tree; settings.json merge idempotent (re-run adds 0 rules) | CONFIRMED | Live: fresh scratch dir, `init --kit engineer` (exit=0, `hooks keys=2`) then `init --kit marketing` (exit=0, 16 copied/14 skipped, both command trees present) then re-run `init --kit marketing` (exit=0, only 3 `SKIP (exists)` lines, **no** "added .* rule" line, `hooks keys after=2` unchanged). Code: `bin/ck.js:57-79` merge-ownership set + copy loop. | Y — phase-01 Gate 3 and plan.md's "two-kit project is the normal case" claim rest on this; wrong → a `ba`+`engineer` project would corrupt settings.json or duplicate hooks on re-init. |
| B3 | `printKitList()` globs `.claude/kits/*.json`, header `📦 Available kits (N):` | CONFIRMED | `node bin/ck.js init --kit list` →<br>`📦 Available kits (3):`<br>`  both         v1.0.0    ...`<br>`  engineer     v1.4.0    ...`<br>`  marketing    v0.1.0    ...`<br>`Usage: ck init --kit <name>`. Code: `bin/lib/kit-resolver.js:130-142`, glob at `:20-21`. | Y — phase-01 Gate 1 asserts this exact header with N=4 after `ba.json` lands; if header format/text differs the gate's grep fails. |
| B4 | `resolveSourcePath` = `path.join(PACKAGE_ROOT, relPath)`; `resolveKit` accepts an external manifest file path | CONFIRMED | `bin/lib/kit-resolver.js:110` `const primary = path.join(PACKAGE_ROOT, relPath);`. External path: `:50-68`, `if (kitArg.endsWith(".json")) { const customPath = path.isAbsolute(kitArg) ? kitArg : path.join(process.cwd(), kitArg); ... }`. | Y — underlies R17 (path.join doesn't contain) and the plan's "option (a) is not buildable" finding; both hinge on this exact join behavior. |
| B5 | `getKitPaths(manifest)` reads only `paths` and `requires`; `extends` inert | CONFIRMED | `bin/lib/kit-resolver.js:85-95`: iterates `Object.values(manifest.paths \|\| {})` and `Object.values(manifest.requires \|\| {})` only, no reference to `manifest.extends`. Live: `both.json` has `"extends": ["engineer","marketing"]` yet `getKitPaths(both).length === 35` — count matches summing `both.json`'s own `paths`+`requires` arrays directly, not derived from engineer/marketing manifests. | Y — phase-01 states "no combination manifest is planned" because of this; wrong → `both.json`'s explicit path duplication would be redundant/dead reasoning. |
| B6 | `listKits()` exposes `.manifest.name`; skips malformed manifest with a warning, not a throw | CONFIRMED (code); not executed | `bin/lib/kit-resolver.js:18-32`: `try { const manifest = JSON.parse(...); return { file: f, manifest }; } catch (e) { console.warn(\`⚠️  Skipping malformed kit ${f}: ${e.message}\`); return null; }` then `.filter(Boolean)`. `.manifest.name` used at `:42,71,73,78,139`. No live malformed-JSON test run (would require writing into `.claude/kits/`, out of scope for read-only verify). | N — cosmetic-error-handling claim, not cited as load-bearing by the plan. |
| B7 | `labelFor('business-analysis-rules.md')` → `"Business analysis rules"` | CONFIRMED | `node -e "const {labelFor}=require('./bin/lib/claude-md-wire.js'); console.log(labelFor('business-analysis-rules.md'))"` → `Business analysis rules`. Fn at `bin/lib/claude-md-wire.js:55-61` (no `LABELS` entry for this file → falls to the generic fallback: strip `.md`, `-`/`_`→space, capitalize first char). | Y — plan.md Global Constraints table and phase-01 Gate 2 assert exactly this string lands in CLAUDE.md; wrong → the naming rationale for the workflow file ("named so labelFor yields X, not Y") is false. |
| B8 | `bin/lib/cli-parser.js:64` and `:94` — help text lists `engineer\|marketing\|both\|<custom.json>` | **REFUTED** | `sed -n '64p;94p' bin/lib/cli-parser.js` → line 64 = `  npx @trungdo9/ClauKit <command>`; line 94 = `  npx @trungdo9/ClauKit init --kit marketing`. The actual `engineer\|marketing\|both\|<custom.json>` text is at **line 75**: `  --kit <name>    Install a specific kit (engineer\|marketing\|both\|<custom.json>)` (confirmed unique match, `grep -n`). File is 98 lines total. | N as asked (this exact citation isn't reused as a gate anywhere I found) — but see note below: the plan itself cites `cli-parser.js:64,94` for a *different*, correct reason. |
| B9 | `bin/lib/github-client.js:47,69` — verify against what phase-10 claims | CONFIRMED | phase-10-defect-record.md:19 cites `bin/lib/github-client.js:47,69` (\`ClauKit-CLI\` User-Agent) as part of the npm-rename blast radius. File: line 47 `    https.get(url, { headers: { "User-Agent": "ClauKit-CLI" } }, (res) => {` (inside `fetchFromGitHubAPI`); line 69 — identical text (inside `fetchTagsFromGitHub`). Both lines hardcode the literal string `"ClauKit-CLI"`. | Y for the npm-rename defect record (10) — if renamed, these two literals need updating; citation is accurate. |
| B10 | `marketing.json` `requires.shared` = exactly 5 entries (planner.md, planning/SKILL.md, html-output.md, 2 workflows), none under `commands/ck/`; full `paths` object | CONFIRMED | `.claude/kits/marketing.json` `requires.shared` (verbatim): `[".claude/agents/engineering/planner.md", ".claude/skills/software/planning/SKILL.md", ".claude/skills/software/planning/references/html-output.md", ".claude/workflows/primary-workflow.md", ".claude/workflows/development-rules.md"]` — 5 entries, 0 matching `commands/ck/`. Full `paths` object pasted below (see appendix); phase-01's `ba.json` mirrors the shape (3 `requires.shared` entries: scenario/SKILL.md + the same 2 workflows). | Y — phase-01's "precedent agrees" argument for `ba.json`'s 3-entry `requires.shared` is built directly on this count; wrong → the precedent claim collapses. |
| B11 | `.claude/kits/` contains exactly `engineer.json`, `marketing.json`, `both.json` | CONFIRMED | `ls -la .claude/kits/` → 3 files: `both.json`, `engineer.json`, `marketing.json`. No others. | Y — B3's "N=3 → N=4" gate and phase-01 Gate 1 both assume the pre-`ba` baseline is exactly 3. |
| B12 | `.claude/hooks/README.md` backticks `.claude/workflows/development-rules.md`, `.claude/workflows/primary-workflow.md`, and `branch-guard.cjs` — line numbers | **REFUTED (partially)** | `grep -n "development-rules.md\|primary-workflow.md\|branch-guard.cjs" .claude/hooks/README.md` → only 1 hit: line 13, `branch-guard.cjs` (both bare and as `.claude/scripts/ck/branch-guard.cjs`). **Neither `development-rules.md` nor `primary-workflow.md` appears anywhere in `.claude/hooks/README.md`** (`grep -c` = 0 for both). `development-rules.md` **is** backticked, but in a different file: `.claude/hooks/discord-hook-setup.md:117` (`` `.claude/workflows/development-rules.md` ``). `primary-workflow.md` is not backticked in **any** file under `.claude/hooks/`. | **Y — this is load-bearing and wrong.** Phase-01 (line 59) and plan.md (line 111) both justify shipping `development-rules.md` + `primary-workflow.md` in `ba.json`'s `requires.shared` with "for marketing's exact reason: `ba` declares `hooks/`, and `.claude/hooks/README.md` backticks `.claude/workflows/development-rules.md`." The backtick is real but lives in `discord-hook-setup.md`, not `README.md`, and there is **no such backtick for `primary-workflow.md` anywhere in `.claude/hooks/`** — so half of this stated rationale has no textual support in the hooks tree at all. (`primary-workflow.md` may still be needed for other reasons — e.g. it's required by the installer-claude-md hard-stop rule, or simply good practice — but "hooks/README.md backticks it" is not one of them, and neither file backticks either workflow as the plan's own wording implies for both.) |
| B13 | `.claude/scripts/ck/plan-lint.cjs` exit codes: 0 clean / 1 findings / 2 usage error | CONFIRMED | Live: `node plan-lint.cjs` (no args) → `exit=2` ("usage: ..."). `node plan-lint.cjs /tmp/nonexistent-dir` → `exit=2` ("plan.md not found in ..."). `node plan-lint.cjs plans/260910-1533-ba-kit/plan.md` → PASS, `exit=0`. Code for the "1 = violations" path: `plan-lint.cjs:52-53`, `if (!warnOnly) process.exit(1);` — not independently exercised (would need a plan that fails lint) but the code path is unambiguous. | Y — plan.md's "Helper CLI contract" for `.claude/scripts/ba/traceability.cjs` states this is the "same shape as `plan-lint.cjs`"; if plan-lint's actual contract differs, the analogy for the new BA script's exit codes is unsound. |
| B14 | `.claude/scripts/ck/phase-brief.cjs` exists; run with no args → usage line | CONFIRMED | File exists (`ls .claude/scripts/ck/` lists it, 3125 bytes). Live: `node .claude/scripts/ck/phase-brief.cjs` (no args) → stdout `✗ usage: phase-brief.js <plan-dir-or-plan.md> <phase-number>`, **real exit code = 1** (verified without a pipe to `head`, which had silently swallowed the true exit code on a first, since-discarded attempt). `die()` call at `phase-brief.cjs:45` passes no explicit code → defaults to 1 per `common.cjs:58` `function die(msg, code = 1)`. | N — not cited as a plan gate directly. |
| B15 | `.claude/skills` is a symlink to `./skills` | CONFIRMED | `ls -la .claude/skills` → `lrwxrwxrwx 1 trung trung 9 ... .claude/skills -> ../skills`. | Y — phase-01's authoring rule ("author under `skills/ba/`, manifest declares `.claude/skills/ba/`") and `resolveSourcePath`'s de-symlink fallback both depend on this being true today. |
| B16 | CLAUDE.md contains "Adding a new kit = drop a JSON file, no CLI changes" | CONFIRMED | `grep -n "Adding a new kit" CLAUDE.md` → `27:Kit manifests: \`.claude/kits/*.json\`. Adding a new kit = drop a JSON file, no CLI changes.` | Y — phase-01's framing ("`CLAUDE.md`'s 'adding a kit = drop a JSON file, no CLI changes' is true again") quotes this sentence as its premise. |
| B17 | `tests/installer-packaging.test.js` hardcodes `['engineer', 'marketing', 'both']` at lines 165, 275, 341 (plan.md "Corrections" table) | CONFIRMED | `grep -n` → `165:  for (const name of ['engineer', 'marketing', 'both']) {`, `275:  for (const kit of ['engineer', 'marketing', 'both']) {`, `341:  for (const kit of ['engineer', 'marketing', 'both']) {`. Exact match, all 3 line numbers and the literal array. | Y — phase-09's entire "derive the kit loop from disk" task is premised on these 3 hardcoded loops existing exactly here; wrong line numbers → phase-09's edit instructions target the wrong code. |
| B18 | `tests/installer-claude-md.test.js:179` — a 4th hardcoded kit loop | CONFIRMED | `sed -n '170,185p'` → line 179: `  for (const kit of ['engineer', 'marketing', 'both']) {`. | Y — same as B17, phase-09 dependency. |
| B19 | `.claude/commands/ck/cook.md:85-86` — Exact-Requirements Gate is UNSKIPPABLE, derives 5 items (expected output, acceptance criteria, scope boundary, constraints, touchpoints) | CONFIRMED | Line 85: `* Read the \`cook\` skill file; run its Stage 0 gate: derive the 5 items (expected output, acceptance criteria, scope boundary, constraints, touchpoints)...`. Line 86: `* The gate is **UNSKIPPABLE**. \`--fast\` and \`--auto\` never bypass it; \`--from-plan\` satisfies it from the plan file.` | Y — plan.md's "compose output is a /ck:cook Stage-0 input" section and its `[ASSUMED]`-count-of-1 metric both rest on this gate's exact wording/behavior. |
| B20 | `skills/software/to-tickets/SKILL.md:26,93` — "one file per ticket, never one combined file" | CONFIRMED | Line 26 (table row): "...one file per ticket, numbered from `01`..."; line 93: `**Local** — one file per ticket, never one combined file:`. | Y — D-6's per-entity spine rationale cites this as precedent; wrong → the analogy to an established in-house convention fails. |
| B21 | `skills/skills-lock.json` = `{"version": 1, "skills": {}}`, unused | CONFIRMED | `cat skills/skills-lock.json` → exactly `{\n  "version": 1,\n  "skills": {}\n}`. Empty `skills` object confirms "unused". | N — informs an "unresolved question" deferral, not a hard gate. |
| B22 | `tests/protected-branch-guard.test.js:196` fails on a clean tree, `actual: 0, expected: 2` | CONFIRMED | Live: `node --test tests/protected-branch-guard.test.js` → test at `:196:1` "spawned in a real repository, the exit codes are the gate" fails: `AssertionError ... actual: 0, expected: 2`. Exact match to plan's wording. | Y — plan.md states every gate in the BA plan uses "fail 1" as baseline (not "fail 0") because of this pre-existing failure; wrong → the plan's gate-command expected outputs would be off by one everywhere. |
| B23 | `npm test` runs 0 tests on Node ≥24 (`node --test tests/` resolves dir as module entry); `node --test "tests/*.test.js"` → 349/347/1/1 | CONFIRMED | `node --test "tests/*.test.js"` → `ℹ tests 349`, `ℹ pass 347`, `ℹ fail 1`, `ℹ skipped 1` — exact match to plan.md's "Repo baseline" table. (Did not separately re-verify the bare `node --test tests/` failure mode live; the counts match is the load-bearing half and is confirmed.) | Y — this exact baseline (349/347/1/1) is what every later phase's gates are diffed against. |
| B24 | No `LICENSE` file on disk; `package.json` declares `"license": "MIT"` and lists `"LICENSE"` in `files` | CONFIRMED | `ls LICENSE` → "No such file or directory". `package.json:28` `"license": "MIT",`; `files` array (`:33-45`) includes the literal string `"LICENSE"`. `git ls-files \| grep -i '^LICENSE$'` → empty (not tracked). | Y — `docs/known-defects.md` entry 1 (phase-10 task 10.1) is built entirely on this inconsistency. |
| B25 | `npm view @trungdo9/ClauKit` → E404 "name can no longer contain capital letters"; `@trungdo9/claukit` → plain 404 (free) | CONFIRMED | Live (network): `npm view @trungdo9/ClauKit` → `npm error 404 This package name is not valid, because` / `npm error 404  1. name can no longer contain capital letters`. `npm view @trungdo9/claukit` → generic `404 Not Found`, no "not valid" / capital-letters message — i.e. the name itself is syntactically valid and simply unregistered (free). | Y — `docs/known-defects.md` entry 2 and Unresolved Q3 are built on this exact distinction. |
| B26 | `plans/260825-1134-kitforge-display-rename/phase-05-verify-and-manual-steps.md:52` asserts `p.name === "@trungdo9/ClauKit"` as a frozen-literal gate | CONFIRMED | `grep -n 'p.name==='` → line 52, `node -e 'const p=require("./package.json");const ok=p.name==="@trungdo9/ClauKit"&&...'`. | Y — plan.md's "direct collision" claim (an npm rename would fail this older plan's own verification step) depends on this literal existing at exactly this line. |
| B27 | `commands/ck/tickets.md` and `to-tickets/SKILL.md` carry the specific markdown-link sets the plan tabulates (phase-01/plan.md "requires.shared correction" table); `scenario/SKILL.md` carries none | CONFIRMED | `grep -oE '\]\([^)]+\.md[^)]*\)'` on `tickets.md` → targets: `ticket-slicer.md`, `to-tickets/SKILL.md`, `plan.md`(×2), `cook.md`(×2), `planning/SKILL.md`, `scout.md`, `refactor.md`, `git.md` — matches the plan's claimed set exactly (8 distinct files). Same on `to-tickets/SKILL.md` → `plan.md`, `cook.md`, `ticket-slicer.md`(×2), `scout.md`, `tickets.md` — matches. `scenario/SKILL.md` → 0 matches (`grep` exit 1), and 0 matches for any `.claude/` backticked path. | Y — this table is the entire justification for cutting 3 of D-12's proposed 6 `requires.shared` entries down to 3; if the link sets were smaller than claimed, the "drags in most of the engineer kit" argument would be weaker. |

## Note on B8 vs B9/plan's own citation

B8 (team-lead's claim) and the plan's own blast-radius citation both name `bin/lib/cli-parser.js:64,94` — but for different content. The plan's citation (phase-10:19, plan.md:25) is correct: those two lines literally contain the string `@trungdo9/ClauKit` in usage/example text (`npx @trungdo9/ClauKit <command>` / `npx @trungdo9/ClauKit init --kit marketing`), which is genuinely part of the npm-rename blast radius. B8's claim — that lines 64/94 hold the `engineer|marketing|both|<custom.json>` kit-list text — is a different, wrong assertion; that text is at line 75. No defect in the plan here, just flagging so the two aren't conflated.

## Appendix — `marketing.json` full `paths` object (verbatim, for B10)

```json
"paths": {
  "agents": [".claude/agents/marketing/"],
  "commands": [".claude/commands/mk/"],
  "skills": [".claude/skills/marketing/", ".claude/skills/automation/", ".claude/skills/integrations/"],
  "workflows": [
    ".claude/workflows/marketing-workflow.md", ".claude/workflows/seo-workflow.md",
    ".claude/workflows/social-workflow.md", ".claude/workflows/multi-site-workflow.md",
    ".claude/workflows/sales-workflow.md", ".claude/workflows/crm-workflow.md",
    ".claude/workflows/video-workflow.md", ".claude/workflows/design-workflow.md",
    ".claude/workflows/marketing-rules.md", ".claude/workflows/automation-rules.md",
    ".claude/workflows/cro-framework.md", ".claude/workflows/editorial-review-checklist.md"
  ],
  "hooks": [".claude/hooks/"],
  "statusline": [".claude/statusline.cjs", ".claude/statusline.sh", ".claude/statusline.ps1"],
  "config": [".claude/settings.json", ".claude/mcp.json", ".claude/.env.example", ".claude/.mcp.json.example"]
}
```

## Summary

27 claims checked. **24 CONFIRMED, 2 REFUTED (B8, B12), 0 UNVERIFIABLE, 1 CONFIRMED-with-caveat (B6, code-only, not executed).** The one substantive defect (B12) is load-bearing: the plan's stated rationale for shipping `primary-workflow.md` in `ba.json`'s `requires.shared` ("hooks/README.md backticks it") has zero textual support anywhere under `.claude/hooks/` — recommend the plan re-source that half of the justification (e.g. cite the installer-claude-md hard-stop rule, or `discord-hook-setup.md` for the `development-rules.md` half only) before phase 01 ships.

## Unresolved questions

1. B6 (malformed-manifest warning path) verified by code reading only — did not execute a live malformed-JSON drop into `.claude/kits/` since that would mutate the real kits directory (out of scope for read-only verify). If a definitive live proof is wanted, run it in a scratch copy of `bin/lib` with `KITS_DIR` monkey-patched, or accept the code citation.
2. B13's "1 = findings" exit path was not exercised live (would need a plan.md that actually fails lint); confirmed by code only (`plan-lint.cjs:52-53`).
3. B23's negative half (bare `node --test tests/` actually failing under Node ≥24) was not re-run live in this session — took the positive half (349/347/1/1 via the working invocation) as the load-bearing, testable claim and confirmed it exactly; the failure-mode description is Node-version-dependent behavior the plan states as background, not a gate.
