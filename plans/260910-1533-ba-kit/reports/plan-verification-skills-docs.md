# Plan verification — SKILLS / COMMANDS / DOCS SEAM

Scope: plans/260910-1533-ba-kit/plan.md claims C1-C14 (given) + C15+ (extracted from phase-03/04/08). Read-only; probes ran in `mktemp -d` scratch dirs only.

| # | Claim | Verdict | Evidence | Load-bearing? (Y/N) — impact if wrong |
|---|---|---|---|---|
| C1 | to-tickets/SKILL.md:26,93 "one file per ticket, never one combined file"; :45 spec first-class source; :134 ticket→cook must carry AC | CONFIRMED* | `:93` verbatim "**Local** — one file per ticket, never one combined file:". `:26` only has "one file per ticket" (table cell), NOT the "never one combined file" clause — that exact phrase is `:93`-only. `:45` verbatim "a spec path → read the file" (supports "first-class source" as paraphrase, not verbatim). `:134` verbatim "a ticket handed to cook must carry its acceptance criteria and link its parent `plan.md`". | Y — precedent citation for BA's one-file-per-entity + spec-as-source rules (phase 03/08); *substance* holds, but claim over-states :26's exact wording |
| C2 | scenario/SKILL.md has NO `](` and NO backticked `.claude/` path | CONFIRMED | `grep -n '](' skills/software/scenario/SKILL.md` → no match. `grep -n '`\.claude/' skills/software/scenario/SKILL.md` → no match. | Y — phase 08 cites scenario as a link-clean `requires.shared` dependency; false positive would break that shared-path plan |
| C3 | tickets.md link targets = {ticket-slicer.md, cook.md, git.md, plan.md, refactor.md, scout.md, planning/SKILL.md, to-tickets/SKILL.md}; argument-hint has `spec-path` | CONFIRMED | `grep -noE '\]\([^)]+\)' .claude/commands/ck/tickets.md` → exactly these 8 distinct targets (plan.md, cook.md dup'd). `argument-hint: [plan-path \| PROJ-NNN \| spec-path \| (empty)] …` line 3. | Y — phase 08's "spec is a valid /ck:tickets input today" claim rests on this |
| C4 | to-tickets/SKILL.md link targets = {ticket-slicer.md, cook.md, plan.md, scout.md, tickets.md} | CONFIRMED | `grep -noE '\]\([^)]+\)' skills/software/to-tickets/SKILL.md` → exactly these 5 distinct targets (plan.md, ticket-slicer.md dup'd). | N — descriptive only |
| C5 | cook.md:85-86 gate UNSKIPPABLE; :36 --from-plan extracts+[ASSUMED]-logs | CONFIRMED | `:86` verbatim "The gate is **UNSKIPPABLE**. `--fast` and `--auto` never bypass it; `--from-plan` satisfies it from the plan file." `:36` verbatim "extract the 5 gate items from the plan file, `[ASSUMED]`-log any missing (auto-set when arg is a plan path)". | Y — phase 08 Gate 3 depends on this exact mechanic for its "1 [ASSUMED] entry" metric |
| C6 | skills/skills-lock.json === `{"version": 1, "skills": {}}` | CONFIRMED | `cat skills/skills-lock.json` → `{\n  "version": 1,\n  "skills": {}\n}` exactly. | Y — plan's "do not adopt in wave 0" call rests on it being genuinely empty/unused |
| C7 | docs/known-defects.md does NOT exist | CONFIRMED | `ls docs/known-defects.md` → `No such file or directory`. | Y — phase 10 creates it fresh; if it existed, task would be an edit not a create |
| C8 | clauKit-registry.md table columns, status/dup marking, /mk: row count | CONFIRMED | Legend (lines 15-21): ✅ Active, 🟡 Scaffold, blank=Naming inconsistency (resolved), 🔁 Intentional cross-pool, ❗ Potential overlap. Skills tables: `\| Name \| Status \| Folder \| Source/Notes/Scope \|` (lines 47,81,108 etc). Agents: `\| Name \| Status \| Model \| File \|` (line 255). Commands: `\| Command \| Description \|` (line 301). Dupes marked via § 4 "Duplicate / Overlap Detection", §4a "True duplicates … none post-cleanup ✅" (lines 405-409) + inline 🔁/❗ per-name. `/mk:` section header line 384 states "(dispatcher, 12)"; counted 12 rows (plan, seo, content, email, ads, cro, research, growth, campaign, leads, nurture, video). | Y — phase 10 uses this exact table shape as the precedent for adding `/ba:` rows |
| C9 | mk/plan.md pre-flight self-contradicting | CONFIRMED | Verbatim lines 6-10: "## Pre-flight (HARD FAIL)\n\n**If `plans/marketing-context.md` is missing, refuse to run and direct user to `/mk:plan`.**\n\nPer .claude/workflows/marketing-rules.md, every `/mk:` command requires the marketing context hub. The only exception is `/mk:plan` itself." — the HARD FAIL block is unconditional (no self-exception coded), while the prose right below claims `/mk:plan` is exempt. Genuine self-contradiction. | Y — this is the exact defect phase-03/04 say the BA kit's rule 6 + `/ba:plan`'s "no pre-flight block at all" design corrects |
| C10 | marketing/README.md `##` headings | CONFIRMED | `grep -n '^##' skills/marketing/README.md` → What's included · Quick start · Commands · Workflows · Claude-SEO Engine ⭐ · SEO Writing Pipeline ⭐ (…) · MCPs (Bring Your Own Server) · User cases · Service domains · Hub context · Related · Source repos (12 H2s; plus nested `###` per workflow/sub-section). | N — shape precedent for BA README, non-blocking |
| C11 | phase-05:52 asserts `p.name === "@trungdo9/ClauKit"` | CONFIRMED | Line 52 verbatim: `node -e 'const p=require("./package.json");const ok=p.name==="@trungdo9/ClauKit"&&…` | Y — this is the frozen-literal gate the BA plan's known-defects entry #2 (npm rename) says collides with |
| C12 | html-output.md, preview dir, plans-kanban/gkg/ck-graphify dirs exist | CONFIRMED | `ls skills/software/planning/references/html-output.md` → exists. `find skills -maxdepth 2 -iname preview` → `skills/software/preview`. `find skills -maxdepth 2 -iname plans-kanban -o … -iname gkg -o … -iname ck-graphify` → all three found (`skills/software/plans-kanban`, `skills/software/gkg`, `skills/software/ck-graphify`). | N — path existence only, supports capability-map redirect rows |
| C13 | .claude/commands/ck/ has design.md, research.md, scout.md, tickets.md | CONFIRMED | `ls .claude/commands/ck/` → includes all four (plus 20 others: ask, bootstrap, brainstorm, claude-md, cook, debug, docs, find, fix, flow, git, health, journal, plan, port, refactor, review, security, sepay, team, test, use-mcp, watzup). | N |
| C14 | .claude/workflows/marketing-rules.md `##` headings | CONFIRMED | `grep -n '^#'` → # Marketing Rules; ## 1 Hard-fail: marketing context hub required · 2 YAGNI/KISS/DRY for content · 3 Quality gates · 4 CRO framework · 5 No code steps in marketing workflow · 6 Output conventions · 7 Anti-patterns (auto-reject) · 8 Editorial Review & Quality Gate (16 tiêu chí) · 9 Safe CMS Publishing Protocol · 10 Cross-references. Exactly 10 numbered `##` rules, cross-references last — matches the shape phase-03 says `business-analysis-rules.md` mirrors. | Y — phase-03's "shape mirrors marketing-rules.md" claim depends on this exact 1-10/cross-refs-last structure |
| C15 | (extracted, plan.md:25) "resolveSourcePath probe … transcript in phase 08" | **REFUTED** (location) / underlying tech claim CONFIRMED | `grep -rn resolveSourcePath plans/260910-1533-ba-kit/*.md` → hits only in `plan.md:25,87` and `phase-01…md:11`; **zero hits in phase-08-qc-gap-and-handoff.md** (read in full, confirms no such content — phase 08 is entirely about `/ba:qc gap` + BA→dev handoff). The actual transcript-like measurement lives in `phase-10-defect-record.md:25-33` (Task 10.1b) and `brainstorm-report.md:243-361`. Re-ran the underlying probe live in a scratch dir: `checkKitPathsAvailable()` on an external-relative-path manifest → `['some/external/only/here.md']` (missing, matches `ck.js:51`'s "❌ … references N missing path(s) in package"); `resolveSourcePath(<absolute external path>)` → silently rebased under `PACKAGE_ROOT` (e.g. `/tmp/tmp.xxx/some/…` → `<repo>/tmp/tmp.xxx/some/…`), confirming `kit-resolver.js:109-118`'s exact behavior as described. | Y — the underlying finding (D-5 item 1, "option (a) not buildable without CLI change") is real and independently reproduced; only the **cross-reference location** ("phase 08") is wrong — a reader following that pointer finds nothing |
| C16 | (extracted, phase-03:29) "in-house precedent: `seo-drift` reports `[NO BASELINE]`" | CONFIRMED (citation refined) | Literal token `[NO BASELINE]` does **not** appear in `skills/marketing/seo-drift/SKILL.md` itself (it uses prose: `:115` "No baseline exists for URL \| Say so; suggest running `baseline` first."). It **does** appear verbatim, attributed to seo-drift's `compare` action, in `.claude/commands/mk/seo.md:42` ("`compare` on a URL with no stored baseline reports `[NO BASELINE]` — it does not invent one") and `.claude/workflows/seo-workflow.md:22,52`. Precedent is real, just sourced one layer up (command/workflow, not the skill file). | N — precedent still valid for BA rule 1, just cite `mk/seo.md:42` not `seo-drift/SKILL.md` |
| C17 | (extracted, phase-04:61) "plan.md § Unresolved Q6" | **REFUTED** | `grep -n Q6 plan.md` → no output. Plan's "## Unresolved questions" (line 426) numbers only **1-5** (Q1 explicit at :430/:211/:248, Q5 explicit at :121/:434). The `/mk:plan` self-contradicting-pre-flight item appears **unlabeled**, inside the "**Closed:**" prose list at `plan.md:436` — never as a numbered/lettered "Q6" anywhere in the file. | Y (minor) — phase-04's own internal cross-reference is broken; a reader looking for "§ Unresolved Q6" finds nothing, though the fact it points to (the self-contradiction) is itself real per C9 |
| C18 | (extracted, phase-03:33,44) precedents: rule 3 draft-default ← `/ck:tickets` opt-in tracker; rule 8 one-file-per-entity ← `to-tickets` | CONFIRMED | tickets.md:19 "TARGET: local files (default) · `--jira PROJ-NNN` publishes sub-tasks under that parent issue"; to-tickets/SKILL.md:27 "**Tracker** (opt-in) \| sub-tasks under a named parent issue \| only when the user names a parent issue **and** approves the publish". One-file-per-entity: to-tickets/SKILL.md:26,93 (see C1). | N — precedents hold |
| C19 | (extracted, phase-08:32-33,94) own citations: tickets.md argument-hint spec-path + "any other `.md` path → that spec file"; to-tickets:45; cook.md:86,36 | CONFIRMED | tickets.md:28 verbatim "\| any other `.md` path \| that spec file \|" (source table). Argument-hint spec-path confirmed under C3. to-tickets:45 confirmed under C1. cook.md:86,36 confirmed under C5. Phase-08's own file:line citations are all accurate. | Y — phase 08's core "spec is a first-class ticket source, zero glue code" claim depends on all four holding, and they do |
| C20 | (extracted, phase-04:61) `.claude/commands/mk/seo.md` exists (shape precedent for `/ba:plan`) | CONFIRMED | `ls .claude/commands/mk/seo.md` → exists. | N |

\* C1 verdict is CONFIRMED-with-caveat: every sub-claim's *substance* holds, but the compound claim implies both :26 and :93 carry the identical phrase "never one combined file" — only :93 does.

## Summary

14/14 given claims (C1-C14): **13 CONFIRMED outright, 1 CONFIRMED-with-caveat (C1: :26 lacks the exact "never one combined file" clause the claim attributes to it)**. 0 REFUTED, 0 UNVERIFIABLE among C1-C14.
6 additional claims extracted from phase-03/04/08 (C15-C20): **4 CONFIRMED (2 with a citation refinement), 2 REFUTED** — both refutations are broken internal cross-references *within the ba-kit plan itself* (plan.md:25 mispoints to "phase 08" for the resolveSourcePath transcript; phase-04:61 mispoints to a nonexistent "plan.md § Unresolved Q6"), not falsehoods about the underlying technical facts, which independently checked out (resolveSourcePath probe re-run live; the self-contradicting pre-flight is real per C9).
Total: 17 CONFIRMED / 2 REFUTED / 0 UNVERIFIABLE / 1 CONFIRMED-with-caveat, across 20 claims.

## Unresolved questions

- Whether the plan's authors want C15/C17 (broken cross-refs) fixed before build starts, or left as-is since the facts they point to are independently true — not something this read-only pass can decide.
- C1's :26 wording gap (table cell says "one file per ticket" only) is cosmetic against the skill's own intent (§ "Publish" at :93 is unambiguous) — flagging in case downstream BA rule-authoring quotes :26 verbatim expecting the fuller phrase.
