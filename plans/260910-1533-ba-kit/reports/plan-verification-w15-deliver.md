# Verify-plan — 08.2 (`/ba:deliver`) + 08.3 (`/ba:spec bp`) — COMMAND/SKILL/DOCS surface

Read-only, HEAD 3ec8f78. Group = files these two phases build on (spec.md, capability-map.md,
traceability.cjs, ba.json, agents, tests dir, business-analysis-rules.md, ck/software cross-refs).
`/ba:diagram` claims (phase 07) explicitly out of scope, marked UNVERIFIABLE-until-07 where hit.

## Claims table

| # | Claim (phase:line) | Verdict | Evidence | Load-bearing? | Impact if wrong |
|---|---|---|---|---|---|
| 1 | 08.2:122 "`/ck:docs` ... target docs/deployment-guide.md ... verified at `.claude/commands/ck/docs.md:40`, which marks that guide **optional**" | CONFIRMED | `.claude/commands/ck/docs.md:40` = `` - `docs/deployment-guide.md` *[optional]* — deployment guide `` | Yes — golive.md template wording depends on this | Template would wrongly promise a file most projects lack |
| 2 | 08.2:108 "only `release notes` strings under `skills/software/**` are `research/SKILL.md:39`, `design/mermaidjs-v11/SKILL.md:88`" | CONFIRMED | `grep -rniE "release notes" skills/software/` → exactly 2 hits, `research/SKILL.md:39: - Review changelog and release notes for version-specific information` and `design/mermaidjs-v11/SKILL.md:88: - [Mermaid GitHub](...) ... v11 release notes` | Yes — justifies adding `release:` key with no existing-capability collision | If a 3rd hit existed, "nothing to redirect to" claim breaks |
| 3 | 08.2:108 "`to-tickets`, `planning` and `plans-kanban` carry zero hits" (for "release") | CONFIRMED | `grep -rniE "release" skills/software/to-tickets skills/software/planning skills/software/plans-kanban` → no output, exit 1 | Yes | same as #2 |
| 4 | 08.2:281 Gate 5 cites `installer-packaging.test.js:292` for the "no `.claude/agents/…` path" guard | CONFIRMED (test exists, name matches) | `tests/installer-packaging.test.js:292: test('no shipped doc names a .claude/ path the install does not have', () => {` | Yes — Gate 5's `docs-manager`/`database-admin` bare-name rule leans on this guard existing | If the test didn't exist, "Gate 5 checks for it directly" is false |
| 5 | Phase-03 Gate 3 awk regex `/^\| *[0-9]+ *\|/` does not match a `D`-prefixed row (08.2:166) | CONFIRMED | Isolated repro: `printf '\| D1 \| ...' \| awk -F'\|' '/^\| *[0-9]+ *\|/{n++} END{print n+0}'` → `0`; same awk on a `\| 12 \|` row → `1` | Yes — this is the entire mechanism by which `rows=55` "survives" | If regex matched D-rows, `rows` would inflate and phase-03's committed Gate 3 would fail |
| 6 | Current `skills/ba/capability-map.md`: `rows=55 unmapped=0 no-gloss=0`, distinct `/ba:` dispatchers `=8` | CONFIRMED | `awk -F'\|' '/^\| *[0-9]+ *\|/{...}'` → `rows=55 unmapped=0 no-gloss=0`; `grep -oE '\`/ba:[a-z]+' \| sort -u` → 8 (api, diagram, export, plan, prd, qc, reverse, spec) | Yes | Baseline the whole D-13 block edits from |
| 7 | capability-map.md does not already have a second table | CONFIRMED (not found) | `grep -n '^## '` → only one hit, line 61 `## Deliberately not in this kit`; file is 68 lines total, H1 + one 55-row table (lines 5-59) + one 4-row cut table (lines 63-68) | Yes | If a `## Delivery-governance` section pre-existed, task 8.2.11 would collide |
| 8 | Where can 8.2.11 insert the D-13 table without breaking `grep -c '^\| \`/'` = 4 | ANSWERED, not a claim | Cut-table rows start `\| \`/delegate\` \|` etc. (backtick-slash literal in col 1); D-13 rows start `\| D1 \|` — column 1 never begins `` \`/ `` regardless of insertion point (before or after `## Deliberately not in this kit`). Confirmed: `grep -c '^\| \`/'` on current file = `4`, and no D-row text anywhere matches that anchor | — | — |
| 9 | `.claude/commands/ba/spec.md` current `argument-hint: fr\|nfr\|uc\|us\|ac\|tc\|compose [<project-slug>]`; adding `bp` collides with nothing | CONFIRMED | `.claude/commands/ba/spec.md:3` verbatim; grep for `bp` as an action token → none; `cr` also absent (arrives 08.1, before 08.3 in execution order) | Yes (08.3 task 8.3.3) | A collision would break the dispatcher |
| 10 | `skills/ba/spec/references/` currently has only `compose-format.md`, `entity-bodies.md`; `bp-structure.md` absent | CONFIRMED | `ls skills/ba/spec/references/` → `compose-format.md entity-bodies.md`; `test -e bp-structure.md` → NOT EXISTS | Yes | 08.3 creates it fresh, no overwrite risk |
| 11 | `.claude/agents/engineering/docs-manager.md` and `database-admin.md` exist | CONFIRMED | `ls .claude/agents/engineering/` includes both filenames | Yes — 08.2 task 8.2.8/D8, D7 name them as bare agent refs | If absent, the redirect points at nothing |
| 12 | `.claude/kits/ba.json` declares no `agents` path | CONFIRMED | `paths` object keys: `commands, skills, scripts, workflows, hooks, statusline, config` — no `agents` key | Yes — is the premise for "bare agent name, never a `.claude/agents/…` path" | If `agents` path existed, the bare-name treatment would be wrong |
| 13 | `.claude/kits/ba.json` description reads "5 commands, 8 skills" | CONFIRMED | verbatim: `"description": "BA Kit — ... 5 commands, 8 skills."` | Yes — 08.2 task 8.2.11's "makes it 6 commands" edit target | — |
| 14 | `.claude/scripts/ba/traceability.cjs` current actions = `index, gap, validate, compose` (4) | CONFIRMED | `traceability.cjs:37`: `` if (!['index', 'gap', 'validate', 'compose'].includes(action)) die(USAGE, 2); `` | Yes | 08.2 calls `deliver` the "6th subcommand" — only true once 08.1 lands a 5th (`changelog`); currently would be the 5th |
| 15 | 08.2 task 8.2.9: `deliver` is added to this existing CLI, not a new script | CONFIRMED | Produces list, phase-08.2: "EDITED `.claude/scripts/ba/traceability.cjs` — 6th subcommand `deliver <project-dir> <what> [--force] [--json]`." (separate NEW files are only the two `lib/*.cjs` renderers) | Yes | A new script would change the invocation surface `/ba:deliver` documents |
| 16 | 08.2 task 8.2.9: positional parse unchanged, `<what>` is `rest[0]` | CONFIRMED | `traceability.cjs:33`: `` const [action, projectDir, ...rest] = process.argv.slice(2); `` — shape matches, `rest` already captured | Yes | If parse shape differed, "no argument-parsing rewrite" would be false |
| 17 | `.claude/commands/ba/` currently has `plan.md, prd.md, spec.md` only; `deliver.md` absent | CONFIRMED | `ls .claude/commands/ba/` → 3 files; `test -e deliver.md` → NOT EXISTS | Yes | 08.2 creates it fresh |
| 18 | `tests/ba-deliver.test.js` does not exist; `tests/lib/` does not exist | CONFIRMED | both `test -e`/`test -d` → NOT EXISTS; `ls tests/` shows no `lib/` dir and no `ba-deliver.test.js` | Yes | Confirms 08.2 task 8.2.12 creates a genuinely new file, and phase 09 (not yet run) owns `tests/lib/` |
| 19 | `.claude/workflows/business-analysis-rules.md` currently has 9 `## ` heading lines | CONFIRMED | `grep -c '^## '` → `9`; headings numbered 1,2,3,4,5,6,7,8,10 (no "## 9." exists at HEAD — a stale reservation, see Consistency findings) | No (informational) | — |
| 20 | Neither 08.2 nor 08.3 edits `business-analysis-rules.md` | CONFIRMED | Grep of both phase files for the filename: only 2 hits, both inside 08.2's Gate 5 bash script (a **read**-only link/backtick walk), none in either phase's "Produces" list | Yes | If either phase silently edited rules-file section numbers, it would collide with ruling R12 (owned by the *original* phase-08, a different file — see below) |
| 21 | Deliverable classes (`derived`/`owned`, a `class:` field) do not exist anywhere in shipped BA code/docs yet | CONFIRMED (not found) | `grep -rn "class:" skills/ba .claude/scripts/ba .claude/commands/ba` → no output; `grep -rn "ba-deliverable"` → no output; current `compose` HEADER (`spine-compose.cjs:21-24`) is `<!-- Tài liệu này được sinh tự động ... -->` with no `class`/`ba-deliverable` token at all | Yes | Confirms 08.2 task 8.2.1's header format is a net-new convention, not a rename of an existing one |
| 22 | 08.2 task 8.2.11: capability-map.md H1 "already stale on the command count" (says 12, D-10 cut to 8) | CONFIRMED | H1 verbatim: `# BA capability map — 55 capabilities, 12 commands`; measured dispatcher count = 8 (claim #6) | No (informational, but feeds the H1-rewrite task) | — |
| 23 | 08.2 task 8.2.12: `tests/ba-spine.test.js` "already 216 lines" | CONFIRMED | `wc -l tests/ba-spine.test.js` → `216` | Yes — justifies a new test file instead of extending it | If actually <200, the stated reason for splitting evaporates (though the decision would likely stand anyway) |
| 24 | 08.3 task 8.3.7: the "no shipped ba doc tells the reader to activate a grouped skill" grep guard already exists in `tests/ba-spine.test.js` | CONFIRMED | `tests/ba-spine.test.js:203`: `` test('no shipped ba doc tells the reader to activate a grouped skill', () => { `` | Yes — is the entire justification for "no test" in 08.3 | If the guard didn't exist, 08.3's zero-test decision would leave the new reference file/skill section unchecked |
| 25 | 08.2 task 8.2.8: "`docs-manager`/`database-admin` ... exactly the treatment phase 08 task 8.3 gives `/ck:tickets`" | CONFIRMED | `plans/260910-1533-ba-kit/phase-08-qc-gap-and-handoff.md:57` (inside "## Task 8.3 — Reuse..." starting line 53): "`/ck:tickets` is the opposite case: it cannot be a `requires.shared` entry ... so it is a **documented prerequisite** — name it as a bare slash string and state that the handoff needs `engineer` installed in the same project." | Yes | Mis-citing the precedent would mean 08.2 invents its own convention instead of reusing one |
| 26 | 08.2 Produces: `spine-compose.cjs` "export widening only" — `HEADER`, `rawBody`, `stripHeading`, `finalize` all already exist internally, only `compose` currently exported | CONFIRMED | Currently `module.exports = { compose };` (`spine-compose.cjs:182`); the four named symbols exist at lines 21 (`HEADER` const), 27 (`function rawBody`), 41 (`function stripHeading`), 129 (`function finalize`) | Yes — "no behaviour change" claim rests on this | If any of the four didn't exist, this would be new code, not a widening, and phase-06 Gate 5 risk would be real |
| 27 | **Gate 6's D-13 table check is internally inconsistent** (own finding, not a plan claim) — see Consistency findings | REFUTED (by reproduction) | See below | **Yes, load-bearing** | Gate 6 fails as written if the worked example is followed literally |
| 28 | Current `spine-parse.cjs` `KIND_ORDER` = 9 kinds, no `CR`; 08.3 Gate 4 expects `kinds=10 last=CR` (post-08.1) | CONFIRMED (current state; dependency, not a defect) | `spine-parse.cjs:18`: `` const KIND_ORDER = ['PRD', 'SRS', 'EPIC', 'FR', 'NFR', 'UC', 'US', 'AC', 'TC']; `` — 9 entries, no `CR` | No | 08.3 runs after 08.1 in execution order; not applicable yet |
| 29 | `.gitignore:72` is `!plans/**/deliverables/*.md` | CONFIRMED | `sed -n '72p' .gitignore` → `!plans/**/deliverables/*.md` (exact line) | Yes — 08.2 Gate 4 and 08.3 task 8.3.4 both cite this exact line for "committed, no new rule" | — |
| 30 | capability-map.md row 22 already resolves D2 "already ✅" via `/ba:spec compose` | UNVERIFIABLE / partially REFUTED | Row 22 verbatim (`capability-map.md:26`): `` \| 22 \| Lắp ráp SRS ... \| ... \| \`/ba:spec srs\` \| ba \| W2 \| `` — the row's own command text says `/ba:spec srs`, an action that **does not exist** in current `spec.md` (actions are `fr nfr uc us ac tc compose`). 08.2:173 cites this row as already-✅ for `/ba:spec compose` without correcting the stale `srs` text | No (pre-existing staleness, not introduced by 08.2/08.3, and 08.2 doesn't touch row 22) | Cosmetic — row 22 is stale independent of D-13; worth a one-line fix but not part of either phase's Produces list |
| 31 | Any claim about `/ba:diagram` behavior or `07`'s outputs | UNVERIFIABLE-until-07 | Out of scope per dispatch instructions | — | — |

## Consistency findings

**1. Load-bearing defect: 08.2's D-13 table spec contradicts its own worked example, and the worked-example shape fails Gate 6 as scripted.**

- Task 8.2.11 (`phase-08.2-ba-deliver.md:164`) states: *"Columns `\| # \| § 10i item \| Verdict then \| Resolved by \| Owner \| Wave \|`"* — 6 columns.
- The worked table immediately below it (`:170-184`) is only 4 columns: `\| id \| § 10i item \| resolved by \| owner \|`, with data rows like `\| D1 \| Scope Statement \| \`/ba:deliver scope\` \| ba \|` — no "Verdict then" cell, no "Wave" cell.
- Gate 6 (`:298`) checks column 5 specifically: `` awk -F'\|' '/^\| *D[0-9]+ *\|/{n++; if ($5 !~ /`/ && $5 !~ /agent|skill|spine/) bad++} ...' `` and expects (`:307`) `d-rows=13 unresolved=0`.
- Reproduced both shapes: on the **6-column** shape (matching the "Columns" spec), `$5` = *Resolved by* → `unresolved=0` ✓. On the **4-column** shape (matching the actual worked example an implementer would copy verbatim), `$5` = *Owner* (e.g. `ba`) → every row fails the backtick/agent/skill/spine check → `unresolved=13` (tested on 3 sample rows: `unresolved=3`, i.e. 100% failure rate) ✗.
- **Impact:** an implementer who builds the table exactly as shown in the phase's own worked example (the more concrete, copy-pasteable artifact) produces a table that fails the phase's own Gate 6. This must be fixed before 08.2 is implemented — either add "Verdict then" and "Wave" columns to the worked table, or fix Gate 6's field index to `$4` (Resolved by, in the 4-col shape) and drop the "Verdict then"/"Wave" columns from the spec sentence. Recommend: keep 6 columns (the H1-rewrite task and the "§10i item ... Verdict then" delta-visibility rationale at :164 argue for keeping the old ✅/🟡/❌ visible), and add the 2 missing columns to every one of the 13 worked rows.

**2. Naming collision risk between "phase-08.2/phase-08.3" (this D-13 group) and "Task 8.2/Task 8.3" inside the original `phase-08-qc-gap-and-handoff.md`.**

- STATE.md ruling R12 governs `## Task 8.2` and `## Task 8.3` **inside `phase-08-qc-gap-and-handoff.md`** (the pre-D-13 phase file) — these insert `## 9. Quy trình chuẩn` and `## 10. Không xây lại cái đã có` into `business-analysis-rules.md`, renumbering old `## 10. Cross-references` to `## 11.`. This is unrelated to my group's `phase-08.2-ba-deliver.md` / `phase-08.3-ba-spec-bp.md` files (D-13, wave 1.5).
- Confirmed neither of *my* group's phase files touches `business-analysis-rules.md` (claim #20) — no actual collision — but the plan-numbering scheme (`08.2` phase file vs `Task 8.2` inside phase `08`) is a standing readability trap for anyone cross-referencing STATE.md ledger lines against phase file names. Purely a documentation-hygiene note, not a defect to fix in code.
- Also relevant: the phase-08.2 quote for the `/ck:tickets` precedent (claim #25) correctly resolves to *phase-08's* "Task 8.3" (line 53-57), not to a "task 8.3" inside phase-08.3-ba-spec-bp.md. Verified the line ranges directly to avoid conflating the two.

**3. Execution-order dependencies confirmed intact.** 08.2 assumes 08.1 has already added a `changelog` subcommand + `CR_STATUS` + the `cr` spec action (currently absent, per claims #9, #14 — both correctly *absent* at HEAD, consistent with 08.1 running before 08.2 per STATE.md's recorded order `08 → 08.1 → 08.2 → 08.3 → 09 → 10`). 08.3 assumes 08.2's `owned`-class header convention (task 8.2.1) exists before it writes `bp-structure.md`'s header line — also currently absent, also consistent with ordering.

**4. `.gitignore` — no `entities/*.md` allowlist line yet.** STATE.md records a coordinator ruling that 08.1 task 8.1.9 adds `!plans/**/entities/*.md` beside `:72`'s deliverables line. Confirmed absent at HEAD (`grep -n "entities" .gitignore` → no output). Not a defect — 08.1 hasn't run — but flagging since 08.2 Gate 4 and 08.3 task 8.3.6 both assume a fully-tracked `plans/ba/demo/**` fixture that depends on this line landing first.

## Unresolved questions

1. Should the D-13 table fix (Consistency finding 1) be patched into `phase-08.2-ba-deliver.md` before implementation starts, or left for the implementer to reconcile at build time? Recommend patching the phase file — it's cheaper to fix the spec now than to have Gate 6 fail mid-implementation.
2. `capability-map.md` row 22's stale `/ba:spec srs` action name (claim #30) — pre-existing, not owned by any wave-1.5 phase. Worth a one-line fix (either in 08.2's H1 pass, since it's already touching this file, or parked for phase 10) but not currently in scope for either phase.
