# Plan verification — wave-1.5 (D-13), PROCESS/TOOLING group

Scope: `git diff plan.md`, `git diff phase-09-test-wiring.md`, `STATE.md:41-48`. Read-only. HEAD 3ec8f78 + uncommitted D-13 edits. Repo /home/trung/workspace/project/private/ClauKit.

| # | Claim (location) | Verdict | Evidence | Load-bearing? | Impact if wrong |
|---|---|---|---|---|---|
| 1 | `discoverPhases` per-id resolver regex, plan.md § Phases: `` `plan-checks.cjs` `discoverPhases` (`:79-96`) `` | CONFIRMED | `plan-checks.cjs:79` `function discoverPhases(dir, plan) {` … `:87` `const m = f.match(/^phase-0*(\d+(?:\.\d+)?)[-.]/);` … `:92` `const file = entries.find(f => new RegExp(`^phase-0*${esc}[-.]`).test(f) && ...)` — function body runs exactly 79-96 | Y | Wrong line range → reader can't audit the ambiguity claims below |
| 2 | STATE:43 quotes `plan-checks.cjs:87` and `phase-brief.cjs:58` for the "invisible to 08b" hazard | CONFIRMED | `:87` is `f.match(/^phase-0*(\d+(?:\.\d+)?)[-.]/)` (discovery regex); `phase-brief.cjs:58` is `new RegExp(\`^phase-0*${n...}\\b|^phase-0*${n...}-\`).test(f)` (per-id regex) — both exact lines | Y | Same |
| 3 | STATE:43 "per-id regex `^phase-0*8[-.]` matches `phase-08.1-*` too" (this is `plan-checks.cjs:92` with `esc="8"`) | CONFIRMED | Direct node repro: `new RegExp('^phase-0*8[-.]')` against `['phase-08-x.md','phase-08.1-y.md','phase-08.2-z.md','phase-09-w.md']` → matches `phase-08-x.md`, `phase-08.1-y.md`, `phase-08.2-z.md` (3 of 4) | Y | Understates real ambiguity if refuted |
| 4 | phase-brief.cjs's own per-id regex quoted in § Phases as `` `^phase-0*8\b` `` | PARTIAL — actual text is the full alternation `^phase-0*8\b|^phase-0*8-` at `:58`; the quote in the diff is a truncation, not a misquote (both halves independently match `phase-08.1-…`, so conclusion unaffected) | `phase-brief.cjs:58` verbatim (quoted above) | N | Cosmetic only |
| 5 | § Phases: probe "a dir holding `phase-08`, `phase-08b`, `phase-09` discovers only 8 and 9" | CONFIRMED | Built scratch dir + stub plan.md, ran `discoverPhases` from `plan-checks.cjs` directly → output `8 -> phase-08-x.md` / `9 -> phase-09-w.md`, no id for `08b` at all | Y | If refuted, the whole "picked `08.1` over `08b`" rationale collapses |
| 6 | § Phases: "`phase-brief.cjs <dir> 8` … resolves each to its own file" (implied for 8, 8.1, 9) | CONFIRMED, contingent | Scratch dir with `phase-08-x.md phase-08.1-y.md phase-08.2-z.md phase-09-w.md` (non-empty, `touch`-only files fail — `phase-brief.cjs` `.trim()`s empty content to `null` and dies): `phase-brief.cjs <dir> 8` → source `phase-08-x.md`; `8.1` → `phase-08.1-y.md`; `9` → `phase-09-w.md`. Correct **only because** `readdirSync` returned `phase-08-x.md` before `phase-08.1-y.md` (`'-' < '.'` lexicographically) — `.find()` takes the first match, so this is order-dependent as the ledger says, not a guarantee | Y | A different filesystem/readdir order could pick `phase-08.1-y.md` for phase `8` |
| 7 | Live repo: `discoverPhases('plans/260910-1533-ba-kit', plan.md)` maps `8 -> phase-08-qc-gap-and-handoff.md`, `8.1/8.2/8.3` → own files (STATE:46) | CONFIRMED | Ran the exact `node -e` one-liner plan.md § Phases itself prescribes → `8 -> phase-08-qc-gap-and-handoff.md`, `8.1 -> phase-08.1-cr-kind-and-changelog.md`, `8.2 -> phase-08.2-ba-deliver.md`, `8.3 -> phase-08.3-ba-spec-bp.md` (full 14-id list verbatim matches STATE:46) | Y | This IS the Q6 mitigation check — if it mismapped, phase-08's brief would silently pull 08.1's text |
| 8 | plan-lint result, ledger claims PASS/14 | CONFIRMED | `node .claude/scripts/ck/plan-lint.cjs plans/260910-1533-ba-kit` → `✓ plan-lint PASS — 14 phase(s), all blocks present` | Y | Gate would fail the plan outright |
| 9 | `bin/ck.js:49` "exits 1 on a missing path" (plan.md § Ordering constraint, unchanged context — not new to D-13) | REFUTED on line number, mechanism CONFIRMED | `bin/ck.js:48-55`: `:49` = `const missing = checkKitPathsAvailable(kit);`; `:54` = `process.exit(1);` inside the `if` block | N — pre-existing citation typo, independently triaged 4× already (STATE:8 "`ck.js:49→:54`"; `reports/plan-verification.md:78`; `plan-verification-installer.md:7`; `plan-verification-installer-2.md:8`; `plan-verification-spine.md:27`), mechanism (exits before copy loop at `:67`) confirmed every time | None — already recorded, not touched by D-13 |
| 10 | Suite arithmetic: current (pre-block) baseline is 358/356/1/1 (STATE:46) | CONFIRMED | `node --test "tests/*.test.js"` → `ℹ tests 358` `ℹ pass 356` `ℹ fail 1` `ℹ skipped 1` | Y | If baseline wrong, every downstream +4/+4 arithmetic is wrong |
| 11 | phase-09's exit gate now reads 366/364/1/1 everywhere (no stale 358) | CONFIRMED | `grep -n '358\|366\|364' phase-09-test-wiring.md` → 2 hits, both `366`/`364` (lines 72, 79); zero `358` hits | Y | A stale `358` would make phase-09's gate pass falsely at the old count, undetected |
| 12 | Same for phase-10 | N/A — not found | `grep` for `358\|366\|364` in `phase-10-defect-record.md` → no matches (file states no test-count numbers) | N | — |
| 13 | Frontmatter table: "Thirteen keys — eight required, five conditional" | CONFIRMED | `grep -n '^| \`'` over the key-rule table → 13 rows: `id kind project title doc parents source confidence out_of_scope touches status impact release` (lines 192-204), same order the claim states | Y | Miscount → validator/`REQUIRED_KEYS` contract mismatch goes unnoticed |
| 14 | plan.md `ITEM_ID`/`KIND_ORDER` == phase-08.1 task 8.1.1's quote | CONFIRMED, identical | plan.md: `ITEM_ID = /^(EPIC|FR|NFR|UC|US|TC|CR)-\d{3}$|^AC-\d{3}\.\d{1,2}$/`, `KIND_ORDER = ['PRD','SRS','EPIC','FR','NFR','UC','US','AC','TC','CR']` — phase-08.1.md Task 8.1.1 same regex/array verbatim (whitespace differs only) | Y | Divergent regex between plan and phase = implementer builds the wrong contract |
| 15 | `PARENT_KINDS.CR` == `EPIC, FR, NFR, UC, US` in both plan.md ID-scheme table and phase-08.1 | CONFIRMED | plan.md: `\| CR \| CR-001 \| EPIC, FR, NFR, UC, US \| ...`; phase-08.1 task 8.1.1: `PARENT_KINDS.CR = ['EPIC', 'FR', 'NFR', 'UC', 'US']` — same 5, same order | Y | Same as above |
| 16 | Phases table gained 08.1/08.2/08.3 rows; "Depends on" cells match each phase file's own line 3 | CONFIRMED | Table cells: 08.1→`02, 06, 08`; 08.2→`08.1, 06, 08`; 08.3→`07, 08.2`. Phase-file line 3: 08.1 "`Depends on:` 02 …, 06 …, 08 …"; 08.2 "`Depends on:` 08.1 …, 06 …, 08 …"; 08.3 "`Depends on:` 07 …, 08.2 …" — all match | Y | Mismatched deps → orchestrator sequences phases wrong |
| 17 | STATE:44 "phase-03 Gate 3's `awk` row regex does not match `D`-prefixed rows" | CONFIRMED | Piped a literal `\| D1 \| x \| y \| \`/ba:deliver uat\` \| ba \| W1.5 \|` row through the exact Gate-3 awk (`/^\| *[0-9]+ *\|/`) → `rows=0 unmapped=0 no-gloss=0`; a plain numeric row (`\| 1 \| ... \|`) → `rows=1` for contrast | Y | If it matched, capability-map rows would jump 55→68 and phase-03's committed `rows=55` gate would silently break |
| 18 | STATE:44 "distinct dispatchers ≤ 8 is SUPERSEDED by D-13 … re-asserted ≤ 9 by 08.2 Gate 6", phase-03 itself untouched | CONFIRMED | `git diff --stat phase-03-rules-readme-capability-map.md` → empty (zero changes); phase-03:125 still reads `≤ 8`; phase-08.2:294-307 (Gate 6) explicitly re-runs the same commands, gets `9`, and states "phase 03's Gate 3 asserted `≤ 8`, which was true when it ran and is superseded by D-13" | Y | If phase-03 had been edited, that's a hard violation of "committed phases 01-08 are not edited" |
| 19 | STATE:47 "`[UNRENDERED]` … is a pass under rules § 4" | CONFIRMED | `business-analysis-rules.md:19-21`, § 4: "Render gate. No renderer available ⇒ ship the source, label it `[UNRENDERED]`, never present it as verified." | Y (supports ruling) | Wrong → ruling has no textual basis |
| 20 | STATE:48 "`.gitignore:72`'s deliverables line" | CONFIRMED | `.gitignore:72` = `!plans/**/deliverables/*.md` | Y | Wrong anchor → new allowlist line lands in wrong place / comment references nonexistent line |
| 21 | STATE:48 "Index still ignored by `plans/**/*.derived.json` (`:69`)" | CONFIRMED | `.gitignore:69` = `plans/**/*.derived.json` | Y | D-9 (generic ignore rule) integrity depends on this line staying put |
| 22 | New phase files (08.1/08.2/08.3) "TRACKED by the root `.gitignore` allowlist" (§ Phases, STATE:46) | CONFIRMED | `.gitignore:62` = `!plans/*/phase-*.md`; `git status --porcelain` lists all three new phase files as `??` (untracked-but-not-ignored — ignored paths never appear without `--ignored`) | Y | If actually ignored, the phase files could never be committed |
| 23 | STATE:47 "phases 05/07 set the precedent" (subagents hand-author demo fixtures, never invoke `/ba:` commands) | UNVERIFIABLE (narrow) | Grep for "hand-authored"/explicit precedent language in phase-05/07 found nothing; indirect support only — both phases' exit gates invoke `traceability.cjs` directly against pre-existing `plans/ba/demo` files, never a `/ba:` command | N | Side justification only, not a mechanism claim |
| 24 | "The brief that requested them called `release` the 11th key" (plan.md:188) | UNVERIFIABLE | No brief/§10i document exists in `plans/260910-1533-ba-kit/` (only `brainstorm-report.md`, pre-D-13); the brief is presumably an out-of-band user message, not a committed file | N | Rhetorical framing only; the substantive count ("13th") is independently confirmed by claim 13 |

## Consistency findings

- **"08.1 Gate 0" is referenced but never instantiated.** plan.md § Phases (line ~203 of the diff) says "so **08.1 Gate 0 below is the check**" for the `discoverPhases` mapping probe, and tells the reader to "run it before 08.1 and after 08.3." But `phase-08.1-cr-kind-and-changelog.md`'s own Exit-gate section starts at **Gate 1** and runs to **Gate 8** — no `Gate 0` heading exists anywhere in that file (`grep -n 'Gate 0' phase-08.1…md` → no hits). Worse: `phase-brief.cjs`'s brief = `Global Constraints` section (plan.md lines 98-344, confirmed via `grep -n '^## '`) + the dedicated phase file's full text. § Phases starts at line 384 — **outside** Global Constraints and outside phase-08.1.md. So an implementer working from a generated phase-8.1 brief (the only artifact `/ck:cook`-style dispatch would hand them) never sees this check at all; it exists solely for a coordinator who reads the full `plan.md`. Not a functional defect (the check can still be run manually, and Q6 records the risk), but the "Gate 0" label is undocumented/orphaned relative to the phase file's own gate numbering — a reader auditing phase-08.1.md gate-by-gate would not find it.
- **STATE.md is a live, multi-session ledger, not a stable diff target.** At dispatch, lines 41-48 held the D-13 planner block; re-checked just now, that block is still at the *same* lines 41-48 (append-only convention holds), but the file has grown to 71 lines with phase-06/07 completion entries, rulings R12-R15, and a cross-reference to sibling verify-plan groups ("spine group done… deliver group done… plan group still running" — that's this report). None of lines 49-71 fall in this group's assigned scope (`plan.md`, `phase-09-test-wiring.md`, `STATE:41-48`); flagged for the merging orchestrator only, not verified here.
- **Item 9 (`bin/ck.js:49`) is inherited, not introduced.** The citation predates D-13 (present in the original plan, retained verbatim across the diff's context lines) and has already been triaged as a non-load-bearing line-number typo in 4 prior reports. Re-flagging here only for completeness per the task's explicit ask.
- No other REFUTED claims found in the assigned diffs; the ambiguity claims (items 3, 6) are CONFIRMED as genuine, order-dependent hazards — consistent with the plan's own framing ("known cost", "luck, not a contract").

## Unresolved questions

1. Whether `phase-brief.cjs`'s ambiguous per-id resolution (item 6/§ Consistency finding 1) should get its own coordinator checklist item, since the current mitigation lives only in plan.md prose that no per-phase brief surfaces — flagged, not this group's call to resolve.
2. STATE:47's "phases 05/07 set the precedent" — could be fully confirmed by reading those phases' own coordinator-ruling ledger entries (not re-checked here; low stakes, side justification only).
