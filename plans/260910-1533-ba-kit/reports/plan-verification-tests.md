# Plan verification — TEST INFRASTRUCTURE group

Target: `plans/260910-1533-ba-kit/plan.md`. Read-only. Repo: ClauKit @ main.

| # | Claim | Verdict | Evidence | Load-bearing? (Y/N) — impact if wrong |
|---|---|---|---|---|
| A1 | packaging.test.js:165,275,341 each carry literal `['engineer','marketing','both']` | CONFIRMED | `grep -n "for (const.*\['engineer', 'marketing', 'both'\])" tests/installer-packaging.test.js` → `165:  for (const name of ['engineer', 'marketing', 'both']) {` / `275:  for (const kit of ...` / `341:  for (const kit of ...` | Y — phase 09 task 9.2 targets these exact lines; wrong line = edit lands on wrong code |
| A2 | installer-claude-md.test.js:179 carries same literal | CONFIRMED | `grep -n "for (const kit of \['engineer', 'marketing', 'both'\])" tests/installer-claude-md.test.js` → `179:  for (const kit of ['engineer', 'marketing', 'both']) {` | Y — same, task 9.2 4th row |
| A3 | esm-host.test.js defines `SHIPPED_NODE_DIRS = ['.claude/hooks', '.claude/scripts/ck']` | CONFIRMED | `tests/esm-host.test.js:31:const SHIPPED_NODE_DIRS = ['.claude/hooks', '.claude/scripts/ck'];` | Y — phase 09 task 9.3 R12 rewrites this exact line |
| A4 | `kit === 'marketing'` gates `EXEMPT_SCRIPTS_IN_MARKETING`; `EXEMPT_TARGET`, `EXEMPT_FILE`, a `LINK` regex exist; LINK ≠ `^\.\.?/` | CONFIRMED | `installer-packaging.test.js:273: const LINK = /\]\(([^)\s]+?\.md)(?:#[^)\s]*)?\)/g;` (not `^\.\.?/` form) · `:320 const EXEMPT_TARGET = new Set([` · `:324 const EXEMPT_SCRIPTS_IN_MARKETING = /^\.claude\/scripts\/ck\//;` · `:325 const EXEMPT_FILE = new Set([` · `:351 if (kit === 'marketing' && EXEMPT_SCRIPTS_IN_MARKETING.test(target)) continue;` | Y — phase 09 task 9.3 R10 replaces line 351's condition; wrong name/shape breaks the edit |
| A5 | test titled ~"no shipped doc links to a file the install does not have", installs each kit, resolves relative `.md` links | CONFIRMED | `installer-packaging.test.js:243:test('no shipped doc links to a file the install does not have', () => {` — body (lines 275-284 seen) loops `['engineer','marketing','both']`, calls `init(p, [], kit)`, walks `.claude`, matches `LINK` | Y — this is the guard phase 09/CLAUDE.md's "38 caught" narrative depends on |
| A6 | `package.json` `"test"` = `node --test tests/`; on this Node it runs 0 tests | CONFIRMED (0 of the real 349 tests execute; node:test's own counter reports it as `tests 1/pass 0/fail 1`, not a literal "0" line — noted) | `package.json:11: "test": "node --test tests/",` confirmed. `npm test 2>&1 \| tail -30` (Node v24.14.1) → `Error: Cannot find module '.../tests'` (MODULE_NOT_FOUND), then `ℹ tests 1` / `ℹ pass 0` / `ℹ fail 1` / `ℹ duration_ms 37.9`. None of the 349 real tests run — Node resolves the trailing-slash dir as a require() target and crashes before discovering any file. `node --test "tests/*.test.js"` (glob form) works: `tests 349 / pass 347 / fail 1 / skipped 1`. Matches plan.md:121 verbatim ("0 tests run") and phase-10-defect-record.md:20. | Y — if any gate assumed bare `npm test` reported real counts it would misfire; confirmed moot — see Unresolved Qs, plan already uses glob form everywhere |
| A7 | `tests/lib/` does not exist yet | CONFIRMED | `ls tests/lib` → `No such file or directory`; `find tests -maxdepth 1 -type d` → `tests`, `tests/behavior` only | Y — phase 09 task 9.1 creates `tests/lib/kits.js`; if dir already existed, task framing ("CREATE") would be wrong |
| A8 | `PLAN_RULES` constant exists, governs plans/ gitignore; current entries; is `plans/**/*.derived.json` already present? | CONFIRMED (exists) / CONFIRMED (not yet present) | `bin/lib/gitignore-wire.js:58:const PLAN_RULES = [` — body (58-61): `["plans/**/reports/review-package-*.md", "plans/**/reports/*-brief-*.md"]` (2 entries only, no `derived.json`). Root `.gitignore` plans block (lines 59-68) mirrors: `plans/**/reports/review-package-*.md`, `plans/**/reports/*-brief-*.md`, no derived.json line. `grep -n derived .gitignore bin/lib/gitignore-wire.js` → no hits. Consumers: `tests/installer-packaging.test.js:26` (`require`), `:78`, `:81`, `:148` (asserts each rule present, and rule NOT applied to `.claude/.gitignore`). | Y — phase 02 task 2.9 / gate 6 add a 3rd entry to this exact array in this exact file; plan's `PLAN_RULES[0]` off-by-index assumption in `installer-packaging.test.js:81` also depends on this exact array |
| A9 | relocate-scripts.test.js has a fixture described as "a kit that installs no scripts" | CONFIRMED | `tests/relocate-scripts.test.js:117: // A kit that installs no scripts (marketing) must leave the old tree alone —` — test itself (`:118-124`) titled `'the helper is never deleted before its replacement is on disk'`, installs `--kit marketing`, asserts script dir untouched | Y — phase 09 task 9.3 R10 comment says the derived `shipsCkScripts` exemption is required so this fixture doesn't break; confirms marketing is currently the no-scripts kit this fixture exercises |
| A10 | `bin/lib/cjs-migrate.js` has a `SHIPPED_JS` table whose digests are resolved against git history | CONFIRMED | `bin/lib/cjs-migrate.js:73:const SHIPPED_JS = {` — comment above (`:66-72`) gives regen recipe via `git rev-list --all` / `git rev-parse "$c:<path>.js"`. `tests/esm-host.test.js:249:test('the SHIPPED_JS digests are real blobs of the real history', () => {` (comment `:252`: "each digest is resolved against git"). Also `installer-packaging.test.js:177` references same. | Y — phase 09 task 9.3 R12 explicitly says "do NOT add anything to SHIPPED_JS — that table is historical provenance resolved against git" citing this exact mechanism; confirms the constraint is real, not invented |
| A11 | protected-branch-guard.test.js:196 test "spawned in a real repository, the exit codes are the gate" currently fails, actual 0 expected 2 | CONFIRMED | `tests/protected-branch-guard.test.js:196:test('spawned in a real repository, the exit codes are the gate', () => {`. Isolated run: `node --test tests/protected-branch-guard.test.js 2>&1 \| grep -E 'not ok\|actual\|expected\|^ℹ (pass\|fail)'` → `ℹ pass 19` / `ℹ fail 1` / `actual: 0,` / `expected: 2,` | Y — plan (phase-09 gate 1) treats this as the one pre-existing baseline failure; if it passed or a different test failed, the "fail 1 is pre-existing, a 2nd fail fails the gate" logic breaks |
| A12 | `tests/ba-spine.test.js` does not exist yet | CONFIRMED | `ls tests/ba-spine.test.js` → `No such file or directory` | Y — phase 02 task 2.7 "CREATE"s this file; consistent |
| A13 | Full suite baseline is 349 tests / 347 pass / 1 fail / 1 skipped (phase-09's stated baseline, and its exit-gate delta "+9 → 358/356/1/1") | CONFIRMED (baseline only) | `node --test "tests/*.test.js" 2>&1 \| grep -E '^ℹ (tests\|pass\|fail\|skipped)'` → `ℹ tests 349` / `ℹ pass 347` / `ℹ fail 1` / `ℹ skipped 1`. Matches phase-09's own stated "(baseline 349/347/1/1...)" exactly. The projected post-phase `358/356/1/1` is NOT verifiable now (depends on ba-spine.test.js's 9 tests, not yet created) — UNVERIFIABLE for the projection, CONFIRMED for the baseline it's computed from. | Y — every phase-09 gate script (`grep -E '^ℹ (tests\|pass\|fail\|skipped)'`) is pinned to this exact baseline; if baseline drifts before phase 09 lands, its gate 1 gets a false fail |
| A14 | `bin/lib/kit-resolver.js` exports `listKits()` and `getKitPaths()` (phase-09 Interfaces: consumes these) | CONFIRMED | `bin/lib/kit-resolver.js:18:function listKits() {`, `:85:function getKitPaths(kitOrManifest) {`, `:144:module.exports = {` block includes `listKits` (`:146`) and `getKitPaths` (`:148`) | Y — phase 09 task 9.1's `tests/lib/kits.js` is written directly against these two signatures; a name/shape mismatch breaks `packagedKits()`/`shipsCkScripts()` on day one |
| A15 | `installer-packaging.test.js` has a test titled ~"the plan-artifact rules stay in sync with ClauKit's own root .gitignore" (referenced by phase-02 task 2.9 Edit 2) | CONFIRMED | `installer-packaging.test.js:144:test('the plan-artifact rules stay in sync with ClauKit\'s own root .gitignore', () => {` | Y — phase-02 task 2.9 "Both or neither" claim rests on this test actually asserting sync; if absent, adding the derived.json line to only one file would go uncaught |
| A16 | `.claude/kits/ba.json` does not exist yet (consistent w/ phase-02 depending on phase 01, and phase-09's "ba is in the package now (D-12)" being prospective) | CONFIRMED | `ls .claude/kits/ba.json` → `No such file or directory`. Existing manifests: `.claude/kits/{both,engineer,marketing}.json` only (from A7 sibling check) | N — informational; plan already declares phase-02/09 depend on phase 01 producing this file, so its current absence is expected, not a defect |

## Summary

Confirmed: 16 of 16 core claims (A1-A12 + A14-A16, plus A6 and A13's baseline half). Refuted: 0. Unverifiable: 1 sub-part (A13's post-phase-02 projected 358/356/1/1 — `tests/ba-spine.test.js` doesn't exist yet, settle by rerunning `node --test "tests/*.test.js"` once phase 02 lands).

## Unresolved questions

- A6 self-resolved: `grep -rn "npm test" plans/260910-1533-ba-kit/*.md` shows the plan already documents this exact defect verbatim (`plan.md:121`, `phase-10-defect-record.md:20`) and states every gate uses `node --test "tests/*.test.js"` instead. No phase doc invokes bare `npm test` as a gate. Not a plan defect — plan's own framing matches my measurement exactly (down to the error text and 349/347/1/1 numbers).
- A13 projection (358/356/1/1) is only checkable after phase 02 exists; flag for re-verification once `tests/ba-spine.test.js` lands.
