# PR payload — feat/durability-evidence-cost → main

Emitted 2026-08-01 by `/ck:git pr` after `gh` auth failed (exit 4, "not logged into any GitHub hosts").
Finish-branch rule 5: paste-ready, zero retries, never a dead end. Branch is pushed and current — only the PR is missing.

## Branches

- base: `main` (`cdbe5e0`) · head: `feat/durability-evidence-cost` (`64c3a8b`, pushed)
- 19 commits · 126 files changed, 8894 insertions(+), 517 deletions(-)
- fast-forwardable: HEAD..main = 0 commits behind

## Title

```
feat(durability): evidence gates, concurrency guard, worktree fleet, declared delivery tail
```

## Command (run after `gh auth login`)

```bash
gh pr create --draft --base main --head feat/durability-evidence-cost \
  --title 'feat(durability): evidence gates, concurrency guard, worktree fleet, declared delivery tail' \
  --body-file plans/260730-1359-clauKit-upgrade/reports/pr-payload-body.md
```

Add `--ready` in place of `--draft` to open it undrafted.

## Body

Verbatim in [pr-payload-body.md](pr-payload-body.md) — filled per the `pr-body.md` contract (8/8 placeholders transcribed from artifacts, 0 remaining).
