# Verification: Group 5 Global Constraints (kitforge-display-rename)

Read-only fact-check, no edits made.

| # | Claim | Verdict | Evidence | Impact if wrong |
|---|---|---|---|---|
| 1 | `.claude/.gitignore:1` and `bin/lib/gitignore-wire.js:38` byte-identical | CONFIRMED | `.claude/.gitignore:1`: `# ClauKit runtime state — machine-local, never useful in history.` / `bin/lib/gitignore-wire.js:38` (inside `HEADER` array): `"# ClauKit runtime state — machine-local, never useful in history.",` — identical text incl. em-dash. | Renaming one without the other silently desyncs generated header from source-of-truth constant. |
| 2 | test ~line 136 asserts the two headers match | REFUTED | Line 136 test is `'the wired rules stay in sync with ClauKit\'s own .claude/.gitignore'` — it compares `RULES` (the pattern array: `.ck-file-claims.jsonl` etc.) via `existingPatterns(own)`, not `HEADER`/the literal text. `grep -n 'HEADER\|ClauKit runtime state' tests/installer-packaging.test.js` → **zero hits**. No test anywhere asserts header-string equality between the two files. | Plan may over-claim test coverage as a safety net for the header rename; that specific line's assertion protects rule-list drift, not header-text drift. Header could be renamed inconsistently without any test failing. |
| 3 | `docs/clauKit-registry.md` has ~15 referrers | CONFIRMED (undercount in claim) | `grep -rl 'clauKit-registry' --include='*.md' . \| grep -v node_modules \| wc -l` → **23** files (list: `.claude/agents/engineering/security-auditor.md`, `.claude/agents/marketing/copywriter.md`, `.claude/commands/ck/find.md`, `.claude/commands/ck/scout.md`, `CLAUDE.md`, `.claude/workflows/skill-activation.md`, `docs/codebase-summary.md`, `docs/project-overview-pdr.md`, `docs/project-roadmap.md`, `docs/system-architecture.md`, `plans/260730-1359-clauKit-upgrade/plan.md`, `plans/260730-1359-clauKit-upgrade/reports/superpowers-comparison.md`, `plans/260730-1359-clauKit-upgrade/STATE.md`, `plans/260825-1134-kitforge-display-rename/phase-01-readme.md`, `phase-02-repo-brand-docs.md`, `phase-03-docs-folder.md`, `phase-05-verify-and-manual-steps.md`, `phase-06-optional-marketing-attribution.md`, `plan.md` (rename plan itself), `README.md`, `skills/marketing/README.md`, `skills/marketing/seo-programmatic/SKILL.md`, `tests/behavior/README.md`). | "~15" undersells scope — actual fan-out is 23, ~53% more than claimed. Rename/link-update effort for this file underestimated if planned off 15. |
| 4 | dir `plans/260730-1359-clauKit-upgrade/` exists; referenced README.md:93 and roadmap:77 | CONFIRMED | `ls` → `plan.md`, `reports`, `STATE.md` (exists). `README.md:93`: `So this release adds almost no surface: **0 new agents, 0 new commands.** It hardens the loop that already existed. Full rationale: [\`plans/260730-1359-clauKit-upgrade/plan.md\`](./plans/260730-1359-clauKit-upgrade/plan.md).` `docs/project-roadmap.md:77`: `### 0. Durability–Evidence–Cost upgrade — SHIPPED 2026-07-31 (plan \`260730-1359-clauKit-upgrade\`)` | Directory name embeds "clauKit" as a historical plan-id, not a live display string — freezing it (not renaming) is correct; both citing lines confirmed at claimed line numbers. |
| 5 | `bin/lib/github-client.js:47,69` literal `ClauKit-CLI` User-Agent | CONFIRMED | Line 47: `https.get(url, { headers: { "User-Agent": "ClauKit-CLI" } }, (res) => {` — Line 69: identical line repeated. | User-Agent string is an external-facing GitHub API identity; changing it without coordination could affect GitHub-side rate-limit/analytics attribution tied to the old UA string. |
| 6 | `.releaserc.json` releaseRules array; `chore` not in it; docs/README, refactor, style → patch | CONFIRMED | `releaseRules`: `[{"type":"docs","scope":"README","release":"patch"},{"type":"refactor","release":"patch"},{"type":"style","release":"patch"}]`. Only 3 entries, `chore` absent — confirmed NOT present anywhere in file. Per conventionalcommits preset, these 3 explicit overrides map to `patch`; a plain `chore:` commit (no override, and not `feat`/`fix`/breaking) is not covered by any rule and does not trigger a release under this preset's defaults. | If a rename commit is typed `chore:` it produces no release (safe/expected); if typed `docs(README):`, `refactor:`, or `style:` it DOES trigger a patch release — relevant for the plan's commit-type choice during the rename. |
| 7 | `package.json` "files" lists `.opencode/` and `AGENTS.md`; neither exists on disk | CONFIRMED | `package.json` files array (lines 34-44) includes `.opencode/` (line 37) and `AGENTS.md` (line 39). `ls .opencode` → `No such file or directory`. `ls AGENTS.md` → not shown in output (command errored before reaching it: `.opencode` ls failure caused exit 2 and stopped the `&&` chain) — re-verify AGENTS.md independently below. | Stale manifest entries for non-existent paths are harmless to `npm pack` (glob simply matches nothing) but could mislead rename-scope assumptions if someone expects these paths to exist and need renaming. |

## Follow-up: AGENTS.md check (chain aborted early on claim 7, re-run standalone)

`ls AGENTS.md` → `ls: cannot access 'AGENTS.md': No such file or directory` (exit 2). Confirms claim 7 fully: both `.opencode/` and `AGENTS.md` are absent from disk despite being listed in `package.json` "files".

## Unresolved questions

1. Claim 2's underlying intent (freeze-header-because-test-guards-it) needs re-stating in the plan — the actual guard at line 136 is rule-list sync, not header-text sync. Does the plan rely on this test to catch a header-rename mismatch? If so it currently would NOT.
2. Claim 3's "~15" vs actual 23 — is 15 a stale count from an earlier plan draft, or a deliberate underestimate/subset (e.g. excluding the rename-plan's own phase files)? Worth confirming which set the plan intends to update.
3. Should `.opencode/` and `AGENTS.md` be dropped from `package.json` "files" as unrelated cleanup, or are they placeholders for future files intentionally kept? Out of scope for the rename itself but flagged since claim 7 touches this manifest.

