# Plan verification — group 4: CLI + package.json (read-only)

Concurrent session note: `git status --porcelain -- bin/ck.js bin/lib/cli-parser.js` = ` M bin/ck.js` / ` M bin/lib/cli-parser.js` (uncommitted, active `ck convert` feature work). Checked `git diff` for both — all additions are new lines/blocks (header comment line, `convertCommand` require, switch case, `COMMANDS.convert`, `--out` parsing, convert help text). None of the additions land on or before-shift any of the plan's target rename lines. Current on-disk line numbers match the plan's cited line numbers exactly (verified below) — plan's line-targeted diff is NOT stale.

| # | Claim | Verdict | Evidence | Impact if wrong |
|---|---|---|---|---|
| 1 | bin/ck.js: 7 case-insensitive "claukit" hits, at the 7 named spots | CONFIRMED | `grep -o -i claukit bin/ck.js \| wc -l` = 7. Lines: L4 `* ClauKit CLI - Initialize Claude configuration in your project.`; L43 `` console.log(`🚀 ClauKit v${packageJson.version}`); ``; L93 `// ClauKit's CommonJS files used to ship as \`.js\`, which Node parses as ESM in`; L107 `// but only where a content digest proves ClauKit shipped that exact file, and`; L119 `` console.log(`\n   🔧 migrated ClauKit's CommonJS files to .cjs (they broke under "type": "module"):`); ``; L185 `` console.log(`🚀 ClauKit Updater v${packageJson.version}\n`); ``; L188 `const repo = packageJson.repository?.url?.replace(...) \|\| "trungdo9/ClauKit";` | Wrong count/line → phase-04's targeted 2-line edit (L43, L185) could miss or hit wrong lines |
| 2 | cli-parser.js: 4 hits — banner, npx line (x2), claukit line | CONFIRMED (count), minor mischaracterization | `grep -o -i claukit \| wc -l` = 4. Lines: L60 `ClauKit v${packageJson.version}`; L64 `  npx @trungdo9/ClauKit <command>`; L66 `  claukit <command>`; L94 `  npx @trungdo9/ClauKit init --kit marketing`. Plan text says "npx line appears twice" — actually two *different* npx lines (L64 generic usage, L94 a worked example) both containing the substring; total count of 4 is right, wording is loose but harmless (plan's own table at line 42-50 lists all 4 correctly with distinct line numbers) | None — plan's table (not prose) is accurate; only the summary prose is imprecise |
| 3 | tests/esm-host.test.js ~L306 assertion on "migrated ClauKit's CommonJS files" | CONFIRMED | L306: `assert.ok(!/migrated ClauKit's CommonJS files/.test(res.stdout), 'nothing left to migrate');` | If wrong, phase-04's "freeze L119" rationale loses its test-coupling evidence |
| 4 | package.json description = "A CLI tool to initialize Claude agents configuration in your project." | CONFIRMED | exact string match via `node -e` require | Wrong baseline → 4.3's diff "before" text mismatches, patch could fail to apply cleanly |
| 5 | package.json name/version/bin | CONFIRMED | `name=@trungdo9/ClauKit version=1.5.1 bin={"ck":"./bin/ck.js","claukit":"./bin/ck.js"}` | Wrong baseline → "do not touch" guard in 4.3 has nothing to anchor to |
| 6 | npm test baseline 329/328/0/1 | CONFIRMED | `# tests 329 / # pass 328 / # fail 0 / # cancelled 0 / # skipped 1 / # todo 0` (full run, ~161s) | If baseline wrong, phase-04 exit gate's "compare to X" step is miscalibrated and a real regression could be missed or a false regression chased |

## Concurrent-edit collision check

Diffed both files against HEAD. `bin/ck.js` additions: 1 header-comment line (before old L4-10 block), 1 `require` line (before old L43), later a `case "convert":` block (after old L185/223 area) — net effect: 2 lines inserted *before* the rename targets (L43, L185), yet current grep shows those exact same line numbers (43, 185) still matching the plan's citations, meaning the plan's line numbers were already computed against this post-convert-feature state, not a stale pre-feature snapshot. `bin/lib/cli-parser.js` additions: 1 flag-comment line, 1 `COMMANDS.convert` line, 1 `out: null` in-place edit (no line-count change), a 6-line `--out` parsing block, and convert-related help text lines — all land in `parseArgs`/help-body regions distinct from L60/64/66/94, and current grep confirms those 4 lines are unchanged and at the plan's cited positions. **No collision**: the plan's line-targeted diffs for phase-04 are current and safe to apply as-is.

## Unresolved questions
- None — all 6 claims fully verified against live working tree at time of check. Only caveat: bin/ck.js and cli-parser.js are mid-edit by another session; re-verify line numbers immediately before applying phase-04's patch if that session commits/further-edits in the interim.
