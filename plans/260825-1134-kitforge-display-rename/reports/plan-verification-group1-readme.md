# Plan Verification — Group 1 (README.md claims)

Repo: /home/trung/workspace/project/private/ClauKit. Read-only check, no edits made.

| # | Claim | Verdict | Evidence | Impact if wrong |
|---|---|---|---|---|
| 1 | Total case-insensitive `claukit` occurrences in README.md = 82 | CONFIRMED | `grep -o -i 'claukit' README.md \| wc -l` → `82` | Baseline count for rewrite scope would be off |
| 2 | Frozen-regex matches = 19 | CONFIRMED | `grep -oE 'trungdo9/ClauKit\|clauKit-registry\|260730-1359-clauKit-upgrade\|ClauKit-CLI\|`claukit`\|"claukit"\|ClauKit/' README.md \| wc -l` → `19`. Lines hit: 5(x2),7(x2),19(x2),26,54,60,93(x2),396(x2),696,738,756,757,773,792 | "63 to rewrite" figure in phase-01 depends on 82-19=63; confirmed consistent |
| 3 | Heading `## ClauKit vs Other AI Coding Tools` exists + exactly 2 `#claukit` anchor refs | CONFIRMED | Heading at README.md:526. Anchor refs (case-insensitive `#claukit`) at lines 689 and 724 — exactly 2 | Anchor-rename step (heading + both refs) is fully scoped; missing a 3rd ref would break a link, but none exists |
| 4 | JSON-LD SoftwareApplication (name ClauKit) + FAQPage with 8 Q/A entries mentioning ClauKit | CONFIRMED | `"@type": "SoftwareApplication"` @768, `"name": "ClauKit"` @769 (within script block 765–778). `"@type": "FAQPage"` @782 (script block 779–826, closed by `-->` @827). 8x `"@type": "Question"` @785,790,795,800,805,810,815,820 — all 8 questions/answers name-check ClauKit (e.g. Q3 "Can I run multiple Claude Code agents in parallel?" has no ClauKit in the question text but its `acceptedAnswer` says "ClauKit spins up independent Claude Code sessions…", confirmed in answer) | Schema block line ranges (765–827, inside one HTML comment closed at 827) needed precisely for any JSON-LD field-level rename |
| 5 | Line 738 asserts "Version 1.3.0 ships gated workflows" vs package.json version 1.5.1 | CONFIRMED | README.md:738: `ClauKit is MIT-licensed — commercial use is allowed. Version 1.3.0 ships gated workflows that block destructive operations (dirty tree refactors, refactors on `main`, tests-red commits). See […] for safety guarantees and [GitHub Releases](https://github.com/trungdo9/ClauKit/releases) for release history.` package.json:3: `"version": "1.5.1"`. Note: an identical "Version 1.3.0…" sentence also appears in the FAQPage JSON-LD answer at line 822 (duplicate, not mentioned in claim but relevant — stale version string appears twice, not once) | Version-mismatch is a pre-existing doc-staleness bug unrelated to the rename itself, but plan should note it appears twice (738 and 822) if in scope, else explicitly out-of-scope |
| 6 | Line 1 exact text `# ClauKit — The Opinionated Multi-Agent Orchestration Framework for Claude Code` | CONFIRMED | README.md:1 verbatim: `# ClauKit — The Opinionated Multi-Agent Orchestration Framework for Claude Code` | H1 rewrite target text is accurate |
| 7 | Line 26 has npm install URL, line 54 has npx github URL | CONFIRMED | README.md:26: `npm install -g https://github.com/trungdo9/ClauKit.git`. README.md:54: `npx github:trungdo9/ClauKit init`. Both at exact claimed line numbers, no drift | Frozen-URL line numbers match phase-01 doc references exactly |

## Unresolved questions
- Claim 4's "roughly lines 760–825" estimate: actual span is 765–827 (comment closes at 827, two script blocks 765–778 and 779–826). Not a refutation, just noting the real boundaries for anyone editing that block.
- Claim 5 impact note: the stale "Version 1.3.0" string exists in two places (738 prose, 822 JSON-LD answer text) — plan/phase files reviewed here don't scope in a version-string fix; confirm whether that's intentionally out of scope for this rename-only phase.
